# 逻辑与风险评审

| 项 | 内容 |
|---|---|
| 评审对象 | 本仓库：飞书群信号 → 币安 USDT-M 合约 跟单机器人 |
| 评审依据 | 逐行读 `src/` + `tools/`；**下面所有数字都从当前代码里取，并用算术核对过**，不是估计 |
| 阅读约定 | 第 1~4 节是**事实**（现在的逻辑）；第 5~7 节是**判断**（专业意见，已标明） |
| 未验证 | 静态分析。本机不能连交易所、没有真实图库、没有真钱数据；实盘行为一概未验证 |

---

## 一、它是什么

**不是"AI 选币机器人"，是"把博主的口头喊单翻译成有纪律的执行，同时给自己装一套防止自作死的护栏"。**

内核是**信不过自己**：每条设计都在回答同一个问题——
*"如果我（AI / 网络 / 交易所 / 我自己的代码）在这里错了，用户会不会在不知情的情况下亏钱？"*

---

## 二、一条消息的完整流程

```
博主在飞书群发消息
 ①取    官方 API（用户身份令牌）拉；失败回退爬网页（fetch_mode 可切）
 ②门口  关键词表粗筛"像不像信号" → 不像就跳过（闲聊不推进）
 ③分流  结单通报 / 你的指令 / 开单信号 / 闲聊
 ④解析  AI 解析文字 ＋ 本地正则 ＋ 读图，三路并行
 ⑤合并  同一信号的多条消息/图/卡片拼成一份（4 秒窗口）
 ⑥校验  方向与价位是否自洽；止盈是否已被现价越过
 ⑦审批  默认所有开仓都要你在群里点头
 ⑧执行  纸面记账 ＋（实盘时）真的下单 → 挂保护单
 ⑨管理  分档止盈 / TP1 移保本 / 每约 30 秒查裸仓与数量
 ⑩结单  写运行日志 + 审计文件 + 飞书多维表格
```

---

## 三、每关的规则（当前默认值）

| 关卡 | 规则 |
|---|---|
| **取消息** | `fetch_mode="api"`（官方 API 为主）；`browser` 可一键回退 |
| **回补闸门** | 停机后补读：**发出超过 30 分钟**（`CATCHUP_MAX_AGE=1800`）的老信号**只通报不下单**；单次最多真处理 **50 条** |
| **解析** | `ai_first_parse=True`（AI 优先）；正则与 AI **交叉补齐**漏读的止盈档 |
| **读图** | `chart_reader="geo"`（几何 + 模型读数）；`llm`（整图直读）**默认关**，`chart_llm_reads=2`（读两次要一致） |
| **价位校验** | 做多时止损须低于开仓（做空反之）；止损距离必须在 **0.05% ~ 40%**；开仓价与市价偏离 **> 20%** 判为串价；止盈已被现价越过的**直接拒绝** |
| **审批** | `require_approval=True` —— **所有新开仓都要人工确认** |
| **下单** | 单笔 **300U 保证金 × 3 倍 = 900U 名义**；最多同时 **5 笔**；入场市价、止盈止损挂单 |
| **持仓** | **3 档止盈各平 1/3**；**TP1 成交后止损移到开仓价（保本）** |
| **止盈兜底** | 博主没给止盈时，按 **2:1 盈亏比**自动挂一档，**到价全平**（`R_FALLBACK_MULT=2.0` / `PART=1.0`） |
| **限价入场** | 30 分钟（`FILL_TIMEOUT=1800`）未成交 → 撤单并通知 |
| **风控** | 总敞口上限 `5 × 300 = 1500U`；当日**已实现**净亏达 `1500 × 20% = 300U` → 熔断暂停（仍监控、不再开新仓） |
| **轮询** | 主循环 `POLL_SEC=0.5` 秒 |

### 三条自动看门狗

| 看门狗 | 频率 | 管什么 |
|---|---|---|
| **裸仓看门狗** | 每约 30 秒（`NAKED_EVERY=60` 轮） | 真实有仓但**没止损单** → 自动补挂，失败继续告警 |
| **周期对账** | 每约 30 秒 | 纸面数量 vs 交易所真实数量**对不上** → 告警（**只报不改**）；纸面刚变动时给 120 秒宽限（`RECON_GRACE=120`） |
| **失联看门狗** | 6 小时无新消息（`SILENCE_ALERT_H=6.0`） | 可能取消息层失效 → 主动告警 |

另有**外部巡检**（systemd 定时器，每 5 分钟，**不依赖机器人进程**）：新增故障行 / 心跳停摆 / 进程消失 / 状态文件停更 → 推飞书。

---

## 四、六道硬闸门与两个分工

### 硬闸门（代码拦住，不靠自觉）

| # | 闸门 | 实现位置 |
|---|---|---|
| 1 | **默认不碰真钱** | `binance_exec._req()` 是唯一出口：非只读请求 + 影子模式 → **直接抛异常** |
| 2 | **开仓必经审批** | `require_approval=True` |
| 3 | **绝不碰用户手工仓** | 交易所有、纸面没有的仓 → 登记为手工仓 → 开仓/平仓/改止损/挂保护**全部拒绝** |
| 4 | **对账不一致就拒绍新真仓** | 启动对账发现差异 → 阻止**真实开仓**（平仓/改止损不受影响）+ 告警 |
| 5 | **保命先挂后撤** | 改止损必须"先挂新的、确认成功、才撤旧的" |
| 6 | **缺配置就拒绝启动** | 缺必备项 → 大声报错 + 退出（**不静默降级**）；`live_trading=true` 但下单层加载失败 → 同样拒绝启动 |

### 两个分工

**① 配置住在哪** —— 规则：**手写的走 `.env`；机器人自己写的留在 json。**

| 类别 | 住哪 |
|---|---|
| 密钥 / 凭据 / 推送地址 | `.env`（模板 `.env.example`） |
| 杠杆 / 上限 / 监控群 / 实盘开关 / 读图方式 | `runtime_config.json`（**可发聊天指令热加载，不用重启**） |
| 持仓 / 游标 / 成交 / 审计 | `v21/state.json`、`*.jsonl` |

**② 影子模式 vs 实盘** —— 默认 `live_trading=false` 是影子模式：全套逻辑照跑，**一条真单都不发**，只推"我本来会发什么"。两个账本分开记：纸面账（策略演算）与真实账（交易所说的）。

---

## 五、专业评审（**判断**，非事实）

### 🟢 明显专业、值得保留

| 设计 | 为什么专业 |
|---|---|
| **保护单挂在交易所侧**（`STOP_MARKET`） | ⭐ **整个架构最值钱的一条**：机器人宕机、网络断、服务器被撑爆 —— **仓位仍有保护**。很多业余跟单机器人把止损做在自己进程里，一挂就裸奔 |
| **绝不猜价**（读不准就报"未读到"、不下单） | 直接杜绝最致命的一类事故 |
| **唯一出口硬闸门** | 安全放在**必经之路**上，不靠调用方自觉 |
| **保命先挂后撤** | 堵住撤挂之间的裸奔窗口 |
| **对账闸门** | 不一致时**只拦开新仓**（平仓与改止损照旧 —— 那两件是保护动作，不该被拦） |
| **绝不碰用户手工仓** | 边界清晰 |
| **不在主循环里等审批** | `ask_user()` 只把待确认项挂进池子就返回，**不 sleep/不阻塞** —— 你还没回复时，机器人继续扫其他群、继续管持仓。这是对的：把“等人”做成阻塞会停掉整个监控 |
| **外部巡检独立于机器人进程** | 生产系统的基本要求 |
| **对账以交易所为准、异常只报不改** | 交易所是唯一事实来源；自动纠偏会把错的状态静默写成"对的" |

### 🟡 可辩、但必须明说

| 设计 | 专业评价 |
|---|---|
| **开仓必经人工审批** | 开发期完全正确。作为**长期生产设计**有三处代价：① 延迟取决于你在不在看手机，**直接吃掉跟单的时效优势**；② 不可扩展（你睡觉时它不工作）；③ **引入选择偏差** —— 你会挑顺眼的批准，**实际跑的不是原策略** |
| **TP1 后止损移保本** | 常见但有争议。降低方差，代价是**砍掉右尾**（大量本会走完的单被保本扫出）。应作为**需要统计验证的参数**，而不是理所当然的规则 |
| **审批有旁路：已持仓的币不再问** | `REQUIRE_APPROVAL and coin not in open_pos` —— **同一币已在持仓时不再走审批**。影响有限（加仓当前只记点位、不下单），但“所有开仓都要审批”这句话在代码里其实有一个例外，写进了文档就应该说全 |
| **失联告警 6 小时才响** | 对“正在管仓位的系统”偏长。缓解：**保护单在交易所侧**，所以盲区里仓位不会裸奔；但会错过入场时机与博主的离场提示 |
| **没读到止盈就按 2:1 自动挂** | 这已不是"跟单"，是**自己发明一条交易规则**。博主不给止盈，说明他的离场可能基于盘面而非固定 R 倍数 |

### 🔴 专业角度站不住

| # | 问题 | 严重度 |
|---|---|---|
| 1 | **仓位是"固定名义"而非"风险定额"** | 唯一"现在就可能亏超预期"的点，见第六节算术 |
| 2 | **熔断只看已实现盈亏，浮亏不计入** | 五笔都开着、浮亏 -500U 也不会触发；集中平仓时瞬间打穿 |
| 3 | **策略层没有自己的优势** | 见下 |
| 4 | **没有相关性/同向限额** | 5 笔同向山寨 = 一个 beta 赌注，“总敞口 1500U”给的安全感是假的 |
| 5 | **单条消息可以阻塞主循环最多 120 秒** | AI 与读图虽然开了线程，但后面用 `join(timeout=120)` **同步等**；而群扫描是**顺序 for 循环**→ 一条慢消息就 2 分钟内不看其他群。博主扎堆发信号时会积压（后到的信号拿着已经走远的价成交） |

**关于第 3 条**（这是最根本的一条，但工程再好也补不了）：

- **没有自己的 edge**：全部收益来自复制别人的决定；系统的真实价值只有"减少执行损耗 + 减少人为失误"
- **幸存者偏差**：博主战绩是他自己说的；跟一段时间后亏的那些会消失，你看到的永远是活下来的
- **逆向选择 / 抢跑**：信号是**公开发到群里**的，你在群消息到达后**几秒到几分钟**才成交 —— 快市场里这段时间差就是成本
- 所以：**这套系统的定位应该是"执行纪律工具"，不是"策略"**。而"这套跟单在纸面上是否有正期望"这个问题，**目前没有数字**

---

## 六、最实质的量化缺陷（算术，全部已核实）

**仓位怎么定**：`notional = margin × lev = 300 × 3 = 900U` —— **恒定的，完全不看止损远近**。
止损距离的允许范围是 0.05% ~ 40%（代码只拿它做"合理性"判断，**不参与定仓位**）。

| 该笔止损距离 | 名义 | **实际每笔风险** |
|---|---|---|
| 0.05%（系统允许的最小值） | 900U | **0.45U** |
| 1% | 900U | 9U |
| 5% | 900U | 45U |
| 40%（系统允许的最大值） | 900U | **360U** |

**同为“300U 保证金”，风险的「理论上限」相差 800 倍**（0.45U ↔ 360U）——
两端都是系统**允许**的边界值（只拒绝超过 40% 或小于 0.05% 的）。

而**现实常见区间**（止损 1% ~ 10%）也相差 **10 倍**（9U ↔ 90U）。这个量级已经足够说明问题：
同一个“300U 保证金”标签下，每笔实际承受的风险**相差一个数量级**。

后果不是理论上的：

- 当日熔断线是 **300U**，但**一笔**宽止损单就能亏 **360U** —— **一笔单突破全天风控线**；
- 反过来，紧止损的单只冒 0.45U，**等于没做**，却白占一个持仓名额（上限只有 5 笔）；
- 5 笔同时开且都是宽止损 → 实际单日风险上限远超"1500U 敞口"给人的安全感。

**专业做法只有一种**：先定每笔能亏多少（风险预算），再由止损距离**反推**仓位——
`仓位 = 风险预算 ÷ 止损距离`。这样止损近则仓位大、止损远则仓位小，**每笔风险恒定**。

---

## 七、优先改动清单（按性价比）

| # | 改什么 | 为什么排这个位置 |
|---|---|---|
| 1 | **仓位改为风险定额**（每笔固定风险 → 反推数量），并把单笔风险上限写死 | 唯一"现在就可能亏超预期"的点；改动小、收益最大 |
| 2 | 熔断线改成"**已实现 + 浮动**"合计（或加一条最大回撤 kill switch） | 否则熔断形同虚设 |
| 3 | 加**相关性/同向敞口限额**（如同时同方向最多 2 笔） | 5 笔同向 = 一个赌注 |
| 4 | 人工审批**按置信度分级**（高置信自动放行、低置信才问） | 既保留安全，又不废掉自动化 |
| 5 | **2R 兜底止盈改成默认不挂**（只通报"未读到止盈"） | 不替博主发明规则 |
| 6 | 纸面账改由**真实成交**驱动（`userDataStream` / `userTrades`） | 消掉"数量漂移"的根 |
| 7 | 读图先**量出准确率基线**（`tools/eval_charts.py`），再谈换不换 | 没有基线，换与不换都是拍脑袋 |
| 8 | **消除单条消息 120 秒的阻塞**（改成异步/排队，不要 `join` 在主循环里） | 博主扎堆时其他群会盲区；这一条会直接吃掉前面几条的优势 |

---

## 八、未验证声明（不许当成已确认）

1. 本评审**全部来自静态阅读**；未连交易所、未跑真实图、未用真钱验证过任何结论。
2. 币安 `STOP_MARKET` 的真实触发行为，仓库里**没有可核验证据**（账户余额与实盘灰度都未到位）。
3. 第五节第 3 条（策略无 edge、幸存者偏差、抢跑）是**分析判断**，不是从数据得出的结论 ——
   要证伪它需要**这套跟单策略的历史期望统计**，而项目目前没有这个数字。
4. 第七节各项的**工作量与收益排序是判断**，不是实测结果。
5. 本文**自己也没经过实测校验** —— 它是静态阅读的产物。文中所有常量、算术、代码行为均逐个复核过，
   但“这个设计在实际盘面下会怎样”属于推断。
6. **未量的关键数字清单**（有了它们才能把本评审从“判断”变成“结论”）：
   · 跟单策略的历史期望（有没有 edge）
   · 读图准确率基线（`tools/eval_charts.py`）
   · 每笔实际风险的分布（止损距离的真实分布）
   · 实际胜率与盈亏比（决定“移保本”与“2R 兜底”到底是帮还是害）

---

## 附：一句话结论

> **它是一座造得很结实的桥，但没算过这座桥要承重多少，也没测过对面是不是有路。**
>
> 工程护栏（第四节那六条 + 三条看门狗）是**专业级**的；
> 风险量化（第五节 🔴1、🔴2）是**业余级**的；
> 策略有效性（第五节 🔴3）目前是**空白**。
>
> **如果只能改一件事：把仓位从"固定 900U"改成"固定风险、反推仓位"。**
