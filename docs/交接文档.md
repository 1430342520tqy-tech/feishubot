# 飞书跟单机器人 · 交接文档（脱敏版）

> 本文中所有密钥 / IP 已替换为占位符。
> **真实值不进 git**，保存在服务器 `/home/ubuntu/signal-bot/config.json`、`notify.json`，
> 以及本地工作区的《交接文档-2026-09-13.md》（含密钥，不提交）。

---

## 一、目标与硬性要求

**目标**：全自动加密合约跟单机器人 —— 自动盯飞书群 KOL 信号（**文字 + K线图**），
解析出「币种/方向/入场/加仓/止损/多档止盈」，在**币安 USDT-M 永续**下单并动态管理。

**硬性要求**：
1. 先 dryRun（纸面）跑通，用户点头才碰真钱
2. 数据必须真实可核验，**禁止编造**
3. 每一单都要看得懂（币种/开单时间/入场/止损/止盈/结单结果），**做成表格**
4. **只做 USDT 结尾的币种**（`XAUUSD` 是图表写法，币安上是 `XAUUSDT`）
5. 读图必须 100% 正确
6. **四档止盈只按三档做，第四档无视**
7. 信号可能是 1~3 条消息（文案/卡片/图）任意组合，必须合并
8. 通知含：币种/时间/金额/杠杆/止损位/止损点数(不含杠杆)/各档止盈/各档预期收益率（**含3倍杠杆**）
9. 博主发止盈通报时，回报用户自己的持仓
10. 代码更新及时上传 GitHub

---

## 二、已完成 / 未完成

### ✅ 已打通
- 服务器环境（Ubuntu 22.04 + Playwright/Chromium + pm2 + 4G swap）
- 飞书网页登录态（真实账号，存 `fs_bot/` profile，**核心资产**）
- **每群独立标签页**并行监控（杜绝串台）
- 消息级 DOM 抓取 + **message-id 高32位 = Unix 秒** 的精确时间戳
- 图表抓取（惰性加载处理）+ 像素级读图（色块定位 → 合并 → 单次视觉调用）
- DeepSeek 语义解析 + 中文俗称币种识别（闪迪→SNDK、海力士→SKHY 等）
- 区间信号取中点（小数向上取整到 tickSize）
- 纸面持仓管理（分档 1/3 止盈、TP1 后移保本损）
- 群内指令系统
- 健康自检 `health_check.py`、成交明细表 `trade_table.py`
- **消息级去重**（防重启后重复开单）+ **同币种不重复开单**
- **重启不再丢信号**（2026-09-13：启动游标续读 + 回补闸门，见第九节）
- **配置热加载**（2026-09-13：改金额/杠杆/测试模式/监控群**无需重启**，消除重启盲窗）
- **页面永久失联修复**（2026-09-13：页面重开失败不再导致该群静默停止监控）
- **币安只读对账层** `binance_reconcile.py`（2026-09-13：真实账户 vs 纸面，全程只读）

### ❌ 未完成
1. **真实下单层完全没写**（当前 100% 纸面）
2. ~~币安 API Key 未创建~~ → **2026-09-13 已创建并实测连通**（只勾合约交易+读取、禁提现、
   绑服务器 IP 白名单）。⚠️ 但账户 **USDT 余额为 0**，接真单前必须先充保证金。
3. 博主"规则止损"（如 4H 收盘跌破 X）按硬止损近似，未做收盘确认
4. 加仓（DCA）只记录点位，未实现真实加仓下单
5. ~~重启期间信号被跳过丢失~~ → **已修复**（见第九节）。残余限制：DOM 只渲染最近若干条，
   停机久到超出页面可见范围的消息仍读不回，**所以仍应避免频繁重启**。
6. 美股代币中文别名表待用户确认
7. 早期信号止盈未读到（`tps: []`）—— 用户已决定**先挂待办**

---

## 三、交易执行规范（核心）

### 资金参数
| 项 | 值 |
|---|---|
| 单笔保证金 | **300 USDT** |
| 杠杆 | **3 倍** |
| 单笔名义 | 900 USDT |
| 最大同时持仓 | **5 笔** |
| 止盈档位 | **3 档**（第4档无视） |
| 测试模式 | 忽略 5 笔上限（仅测试期） |
| 轮询 | 0.5 秒 |
| 待合并窗口 | **4 秒** |

### 开仓价规则
比较博主点位与当前市价，差值 = (市价 − 博主价) / 博主价：

| 情形 | 动作 |
|---|---|
| 差值 **≤ 2%** | 直接市价、一次满仓 900U |
| **做多**且市价高于博主价 **>2%** | **不追高**：博主价×1.01 挂 150U + 博主价 挂 150U |
| **做空**且市价低于博主价 **>2%** | **不追空**：博主价×0.99 挂 150U + 博主价 挂 150U |
| 做多时市价低于博主价 / 做空时市价高于博主价 | 直接市价，止损不变 |

> 两笔各 150U 是**保证金**（×3 = 各 450U 名义，合计 900U 名义）。

### 区间信号
入场价 = 区间中点 `(A+B)/2`，小数部分**向上取整到该合约 tickSize**。

### 止损止盈
- **开仓全用市价**，**只有止盈止损用限价挂单**
- 止盈每档平 **1/3**
- **TP1 成交后止损立刻移到开仓价（保本损）**
- 止损/止盈**优先用 K 线图上读到的**（博主按图交易）

### 读图规则（必须 100% 正确）
- **红色横线 / 暗红区间 = 止损位**（在开仓价**下面**）
- **开仓价 = 止损上方第一个价格标签**
- **止盈 = 开仓价上方的实线横线**（不限颜色），只取前 3 档，第 4 档忽略
- 判定"是不是止盈线"用**横线像素覆盖率**：实线 ≈ 0.85~1.0；价格轴数字/当前价虚线/开仓标记 < 0.6

### 绝对底线
- **止损和止盈都没读到 → 只发「待确认」，绝不下单、绝不猜**
- 一切数值必须来自真实接口 / 真实图片

### 用户已明确不需要的
- **美股代币休市时不设禁止开单窗口** —— 休市时博主开仓照常跟进（已实测：休市盘口活跃、
  开盘跳空最大仅 0.034%，相对 2~5% 的止损距离可忽略）

---

## 四、监控群与指令

**监控群（5 个）**：机器人开单通知、暴富龙、UA-nurseneil2、颜驰2群、黄金mansoor

**指令生效群（仅这 2 个）**：开单记录、机器人开单通知

| 指令 | 作用 |
|---|---|
| `帮助` | 列出全部指令 |
| `状态` / `持仓情况` | 回报当前所有持仓 |
| `全部平仓` | 立即全部平仓，**无需二次确认** |
| `平仓 <币种>` / `减仓 <币种> <百分比>` | 单币平/减 |
| `修改止损 <币种> <价格>` / `移保本 <币种>` | 改止损 |
| `暂停` / `继续` | 暂停时不动作（仍记录） |
| `修改监控群 群1,群2` | 改监控群（**已热加载，无需重启**） |
| `修改金额 300` / `修改杠杆 3` | 改资金参数 |
| `测试模式 开` / `关` | 是否忽略 5 笔上限 |

---

## 五、已知的坑（别重复踩）

1. **群串台** → 必须每群独立标签页 + 标题匹配校验（`indexOf(群名) <= 3`）
2. **图表抓不到** → 图片惰性加载；只要消息里有图片元素就去 fetch，内容图等 6 秒
3. **重启会丢信号** → ~~启动游标直接跳到最新，停机期间的信号被跳过~~ **2026-09-13 已修复**（见第九节）
   - 附带：开页很慢（单群 45~80 秒，5 群合计约 4.5 分钟），日志里 `标题不符` 累计出现 110 次
4. **通知文本不能列群名** → 否则被自己读到又解析一遍，自我循环。
   ⚠️ 真正的护栏是 `SELF_MARKS` 过滤（`【跟单机器人】` 等前缀）。**新增任何通知都必须带这类前缀**，
   否则机器人会把自己的通知当信号重新解析。
5. **启动时别写 `{"open": []}`** → 会清空已恢复的持仓
6. **`open_pos_ref` 与主循环 `open_pos` 必须同一个 dict** → 否则指令永远说"没有持仓"
7. **自建飞书应用进不了群** → 别在这上面浪费时间，用「网页自动化读群 + webhook 推送」
8. **PowerShell ↔ ssh 传中文/引号会被破坏** → 用 `scp` 传文件，不要在 ssh 命令里嵌中文
9. **本机 python.exe 是 WindowsApps 假壳** → Python 活全在服务器上干
10. **本机沙箱阻止 ssh 创建命名管道** → `git push` 需放宽权限重试
11. **别 grep 服务器上的大 JSON**（cookie/storage 文件）→ 一行几十 KB，输出会被撑爆

---

## 六、常用运维命令

```bash
KEY=~/.ssh/dsb_key   # 本地私钥路径

# 健康自检（首选）
ssh -i $KEY ubuntu@<服务器IP> "/home/ubuntu/signal-bot/venv/bin/python /home/ubuntu/signal-bot/health_check.py"

# 成交明细表（含与真实持仓对账）
ssh -i $KEY ubuntu@<服务器IP> "/home/ubuntu/signal-bot/venv/bin/python /home/ubuntu/signal-bot/trade_table.py"

# 币安只读对账（真实账户 vs 纸面，绝对不下单）
ssh -i $KEY ubuntu@<服务器IP> "/home/ubuntu/signal-bot/venv/bin/python /home/ubuntu/signal-bot/binance_reconcile.py"
ssh -i $KEY ubuntu@<服务器IP> "/home/ubuntu/signal-bot/venv/bin/python /home/ubuntu/signal-bot/binance_reconcile.py --push"

# 进程 / 重启 / 日志
ssh -i $KEY ubuntu@<服务器IP> "pm2 list"
ssh -i $KEY ubuntu@<服务器IP> "pm2 restart dryrun-bot2"
ssh -i $KEY ubuntu@<服务器IP> "tail -40 /home/ubuntu/signal-bot/v21/run.log"
```

> ⚠️ 重启前先停 pm2 再改 `v21/state.json`，否则启动时写入会覆盖修改。

---

## 七、目录结构

```
服务器 /home/ubuntu/signal-bot/
  dryrun_bot2.py      机器人主程序
  config.json         DeepSeek key + binance 只读/合约 key（不进 git）
  notify.json         飞书 webhook（不进 git）
  runtime_config.json 监控群/保证金/杠杆/测试模式（**改动会热加载，无需重启**）
  health_check.py     健康自检
  trade_table.py      成交明细表
  binance_reconcile.py 币安只读对账（真实账户 vs 纸面）
  fapi_symbols.json   897 个 USDT-M 合约规格缓存
  v21/state.json      持仓 + 游标 + 已处理消息去重表
  v21/trades_dryrun.jsonl  纸面成交事件流
  v21/run.log         运行日志
  v21/imgs/           抓到的K线图
  fs_bot/             飞书登录态（核心资产，别删）

GitHub 仓库
  src/dryrun_bot2.py        与服务器同名文件 MD5 必须一致
  tools/health_check.py     健康自检
  tools/trade_table.py      成交明细表
  tools/binance_reconcile.py 币安只读对账
  src/binance_exec.py       真实下单层（Algo 止损 + hedge 双向持仓，默认影子模式）
  tools/read_chart_final.py 图表读取算法
  tools/patch_dedup.py      去重补丁
  tools/stock_open_gap.py   美股开盘跳空实测
  tools/stock_liquidity_offhours.py  美股休市流动性实测
  config/config.example.json
```

---

## 八、下一步待办（优先级）

1. **真实下单层** —— 币安 key 已就绪并实测连通，但**账户 USDT 余额为 0**，
   需用户先充保证金；用户 2026-09-13 决定**先只做只读对账层**（已完成）
2. 真实下单层：市价开仓 + `STOP_MARKET` / `TAKE_PROFIT_MARKET`（`reduceOnly`）+ 持仓状态机 + 交易所对账
   ⚠️ 账户当前是 **双向持仓模式 `dualSidePosition=True`**，下单必须带 `positionSide`，先定用哪种模式
   ⚠️ 手续费 maker 万2 / taker 万5 —— 900U 名义开平一次约 0.9U，约吃掉 TP1 收益的 5~10%
3. 补上加仓（DCA）真实下单
4. 补上博主规则止损（4H 收盘确认）
5. ~~缓解重启丢信号~~ → **2026-09-13 已完成**（方案A 游标续读回补 + 方案B 配置热加载）。
   剩余可选：方案C 开页提速（改用会话直链，预计 5 群 4.5 分钟 → 30 秒内，需先实测 chat_id 直链可用）
6. 美股代币中文别名表（待用户确认）
7. 早期信号止盈未读到（`tps: []`）—— 用户指定**排在最后处理**
8. 延迟优化实测数据回报

---

## 九、停机丢信号：机制与修复（2026-09-13）

### 问题机制
旧代码在 `main()` 启动、每开完一个群页面时执行：

```python
last_id[g] = max(last_id.get(g, 0), max(ids))   # 把游标直接抬到"页面最新一条"
```

游标 `last_id` 是"上次处理到哪条"。这一行让游标从**上次停机前的位置**跳到**当前最新**，
中间那段消息 ID 全部小于新游标 → **永远不被当作新消息 → 静默丢弃，连日志都不留一行。**

### 实测数据（服务器 run.log）
最近一次重启（10:02:56 旧进程最后心跳 → 10:03:00 新进程启动 → 10:07:20 才开始实时监控）：

| 群 | 游标跳前 | 跳后 | 该群盲窗 |
|---|---|---|---|
| 机器人开单通知 | 09-13 01:24 | 09-13 01:24:16 | 57 秒 |
| 暴富龙 | 09-11 21:02 | 09-11 21:02:50 | 105 秒 |
| UA-nurseneil2 | 09-12 23:40 | 09-12 23:40:20 | 180 秒 |
| 颜驰2群 | 09-11 21:01 | 09-11 21:01:05 | 215 秒 |
| 黄金mansoor | 09-12 02:23 | 09-12 02:23:07 | **260 秒** |

> 那次重启实际只吞掉几秒~几十秒的窗口（这几个群当时确实安静）。
> **机制是结构性的：一旦重启窗口内有信号，必丢且无告警。** 该进程已重启 29 次。

### 附带发现
- **`>15 条静默全丢`**：旧代码 `if len(new) > 15: new = []`，一次发现超过 15 条就整批丢弃。
  与重启无关，群里刷屏 >15 条即触发。日志里尚未出现过，属潜伏隐患。
- **页面永久失联**：旧代码 `if page is None: continue`，页面重开失败一次 → 该群**永久停止监控且无提示**。
- 开页脆弱：`标题不符` 累计 **110 次**，是 4.5 分钟开页耗时的主要来源。

### 已实施的修复（方案A + 方案B）
**方案A —— 游标续读 + 回补闸门**
- `adopt_page()`：开页时**不再抬游标**；无历史游标的新群才用"页面最新"初始化。
- 首扫自然回补 `id > 游标` 的积压消息，按时间升序处理。
- **时效闸门** `CATCHUP_MAX_AGE = 1800`（30 分钟）：更老的开单信号**只通报、不下单**，避免拿过期点位追单。
- **数量上限** `CATCHUP_MAX_MSGS = 50`：一次最多真处理 50 条，其余只通报。
- 被拦下的消息**推进游标 + 标记已处理**（永不重复），并汇总推一条
  `【跟单机器人】⚠️ 回补闸门拦下 N 条消息` 给你 —— **不再静默**。
- **逐条推进游标**：进程中途挂掉时只会**重放**（`SEEN` 挡住重复开单），不会丢单。

**方案B —— 配置热加载**
- 主循环检测 `runtime_config.json` 的 mtime，变了就热加载，**不重启进程** → 常见重启场景盲窗归零。
- 移除的群自动关页面释放内存；新增的群只新开 1 个页面（约 1 分钟）。
- `修改监控群 / 修改金额 / 修改杠杆 / 测试模式` 指令现在**立即生效，不需要重启**。

### 残余限制（必须知道）
DOM 只渲染最近若干条消息。**停机久到超出页面可见范围的信号仍然读不回** ——
所以核心原则不变：**尽量别频繁重启；要改配置用指令热加载，不要手动 restart。**

### 方案C（开页提速）—— 实测调研后【不可行】，已放弃
2026-09-13 加了 `CHATID_JS` 只读诊断实测：**5 个群打开后页面 URL 完全相同**，
都是 `https://<租户>.feishu.cn/next/messenger/` —— 这个版本的飞书 SPA **不把会话 id 放进 URL**；
DOM 里能扫到的 `chat*` 属性全是 CSS 类名（`lark-chat`、`chatWindow_*`、`chatContainer`），
**不是会话标识**。所以「会话直链」这条路在此版本走不通。

> 附带结论：**方案B（配置热加载）已经消掉了"改配置要重启"这个主要场景**，
> 重启次数会大幅下降 → 方案C 的收益变小、而风险不变。**建议暂缓**，
> 等真的出现"必须重启又不能等"的痛点再考虑。
> 若将来要做，可行的低风险方向是：把开页里的固定 `sleep` 换成条件轮询（成功率/耗时都受益），
> 因为日志里 `标题不符` 累计 110 次，说明**开页慢的主因是重试，不是导航本身**。

---

## 十、真实下单层 `binance_exec.py`（2026-09-13 新增）

### 安全设计
- **`LIVE[0] = False` 默认（影子模式）**：所有下单函数只记录"将要发什么单"到
  `v21/real_orders.jsonl`，**绝不发送任何 POST/DELETE**。`_req()` 是唯一出口，非 GET 且 LIVE 关闭即抛异常。
- 开关：`runtime_config.json` 的 `"live_trading": true`。
- 自检（不下单）：`python binance_exec.py --selftest` / 只读体检 `--check` /
  影子演练 `--shadow DOGE LONG 0.08438 0.0828 0.09965,0.11703`

### ⚠️⚠️ 最重要的一条：币安已迁移止损类订单到 Algo 接口
**2025-12-09 起，`STOP` / `STOP_MARKET` / `TAKE_PROFIT` / `TAKE_PROFIT_MARKET` /
`TRAILING_STOP_MARKET` 已从经典 `POST /fapi/v1/order` 下线**，旧写法返回
`-4120 Order type not supported for this endpoint. Please use the Algo Order API endpoints instead.`
市价单与限价单未受影响。

> 这一条是**本项目 2026-09-13 实探测到的**（`order/test` 报 `-4120`），不是抄文档。
> 如果照老写法直接上真单，**止损会全部挂不上，等于裸奔**。

**新接口实测参数（第一手，全部用"故意不完整载荷"探测，未创建任何委托）：**

| 项 | 值 |
|---|---|
| 下单 | `POST /fapi/v1/algoOrder` |
| 必填 | `symbol`, `side`, `type`, `algoType`, `quantity`, **`triggerPrice`** |
| ⚠️ 注意 | 是 **`triggerPrice`**，**不是**经典接口的 `stopPrice` |
| 触发价口径 | `workingType=MARK_PRICE`（与纸面逻辑一致，纸面用 premiumIndex 标记价） |
| 撤单 | `DELETE /fapi/v1/algoOrder`，必填 `algoId` 或 `clientAlgoId` |
| 查挂单 | `GET /fapi/v1/openAlgoOrders` |
| **`reduceOnly`** | **会被拒**：`-1106 Parameter 'reduceonly' sent when not required`（双向持仓下本就不该传） |
| `algoType` 合法值 | `CONDITIONAL`（另有非法值返回 `-4500 Invalid algoType`） |
| `closePosition=true` | 支持，但走 GTE 语义，**要求已有持仓**，无仓返回 `-4509` |
| **没有 `/test` 版本** | `POST /fapi/v1/algoOrder/test` → **404** |

> 因为 Algo 接口没有 test 端点，自检改用 **`quantity="0"`** 做参数校验：
> 若返回 `-4003 Quantity less than or equal to zero` 说明**其余参数全部被接受**，
> 且数量为 0 保证不可能成交。这是无持仓情况下唯一安全的验证方式。

### 为什么止损不能用限价单
做多时止损价在市价**下方**。若挂 `SELL LIMIT` 在该价，币安规则是「市价 ≥ 限价即可成交」，
而当前市价本来就更高 → **会立刻以市价成交，等于一开仓就平仓**。
所以止损必须是触发单 `STOP_MARKET`。**止盈可以用限价单**（挂在目标价，吃 maker 万2）。

### 双向持仓模式
按用户 2026-09-13 的决定：同一币种可能同时持多与持空（用户选的博主覆盖山寨/主流/黄金/美股四类）。
所有订单带 `positionSide`（LONG/SHORT），**不传 `reduceOnly`**（已实测被拒）。平仓方向：
平多 = `SELL` + `positionSide=LONG`；平空 = `BUY` + `positionSide=SHORT`。

### 实测结果（2026-09-13）
```
[ OK ] 市价开仓          参数被币安接受  {...MARKET, quantity 10627, positionSide LONG}
[ OK ] 止盈限价          参数被币安接受  {...LIMIT GTC, price 0.08892, quantity 3542}
[ OK ] 止损STOP_MARKET   参数全部被接受（用 quantity=0 挡下，未创建委托）
通过 3/3。订单簿复核：经典挂单 0 笔 / Algo 挂单 0 笔。
```

### 待办
- 分批止盈成交后，Algo 止损的 `quantity` 会变旧 → 必须撤单重挂（`sync_sl()` 已实现）。
- 账户充 USDT 后，需要在真实持仓下验证 `closePosition=true` 这条路（无仓验证不了）。

---

## 十一、2R 兜底止盈 + 信号静默丢弃修复（2026-09-13）

### 11.1 2R 兜底止盈（用户决定）
**背景**：博主有时只给开仓价+止损、不给止盈。
实例 LSK：`Long: LSK | Entry: 0.21144 | SL: 0.1993 | Risk: 0.5%` —— 文字无止盈、消息里也没图，
**任何算法都读不出来**（不是读图失败）。

**规则（用户 2026-09-13 确认）**：这种情况 → 挂止损 + **在 2:1 盈亏比位置自动挂第一档止盈**；
后续博主补了真实止盈位，就**撤掉这档按真实档位重挂**。

- `fallback_tp_2r(entry, stop, dirc)`：`R = |入场 − 止损|`，做多 = 入场 + 2R，做空 = 入场 − 2R。
- 该档只平 **1/3**（`R_FALLBACK_PART`，经 `tr["tp_part"]` 显式指定）；
  常规档位仍按 `1/档数` 平分，**不影响现有行为**。
- **绝对底线不变**：止损和止盈**都**没读到 → 仍然只发「待确认」、绝不下单。
- LSK 实例：入场 0.21519 / 止损 0.1993 → 2R 目标 **0.24697**。
- 博主补止盈后：撤掉兜底档 → 按真实档位重挂 → `filled` 重置 → 推【止盈更新·纸面】。
  同时**放宽了待确认池的并入条件**：持仓中的币也能并入，否则后续止盈消息会被直接丢掉。

**验证（服务器实测）**：
- `--selftest-tp` 单元自检 **6/6 通过**
- `test_2r.py` 集成测试 **14/14 通过**（走真实 `finalize_pending`，已隔离）
  ① 无真实止盈时确实出 2R 档且 `tp_part=1/3`
  ② 博主补真实止盈后替换兜底档、**不重复开单**
  ③ 止损止盈都没读到时**仍然绝不下单**

### 11.2 ⚠️ 信号被"静默丢弃"的 bug（实盘复盘抓到）
**实例（真实发生）**：2026-09-13 13:56:45，监控群「黄金mansoor」收到
`XAUUSD 👀 https://x.com/...`，**图已抓到**（日志 `媒体: 元素=2 已加载=1 抓到图=1`），
但之后**一行日志都没有** —— 既没下单、也没待确认、也没跳过提示。

**根因**：币种识别到了（XAU），但 `direction` 为空 →
`if coin and dirc in ("LONG","SHORT")` / `elif` 两个分支都不匹配 →
**落到函数末尾静默结束**，既不 `log` 也不 `notify`。
另外 `info.type=="manage"` 且无动作时也是静默 `continue`。

**危害**：你**完全不知道**错过了一个看起来像信号的消息。违反"读不到就说读不到"。

**修复**：
- 新增 `else` 分支：任何"没通过信号门槛"的消息都留日志（币种/方向/图数/类型/原文前 100 字）。
- 若"识别到币种 且（抓到图 或 含止损止盈关键词）" → 额外推 **【信号·未能识别】** 通知，
  明确写"未下单，等你确认"。**每群 10 分钟节流**，避免某群狂发图时刷屏。
- `manage` 类消息无明确动作时也留日志。
- 通知前缀 `【信号·…】` 命中 `SELF_MARKS`，不会被自己重新解析。

> **教训**：任何"什么都不做"的分支都必须留痕。静默的 `continue` 和静默的 `else` 是丢信号的温床。

---

## 十二、沟通偏好

动手前先说计划 · 用真实数据验证 · 禁止编造 · 简洁中文报告 · 大改动先征求同意
