# 待办与未解决问题清单（2026-09-15）

> 本文件只记录**问题与计划**，不含任何密钥。
> 完整交接（含凭据、服务器信息）见本地 `交接文档-2026-09-15.md`，**不入库**。
> 历史沿革见 `docs/交接文档.md`。

---

## 零、2026-09-15 晚 复核与修复记录（本节最新，先看这里）

### 0.1 B1 根因**已被推翻并重新定位**（原文档写错了）

原文档判断「内核无 OOM → 更像 Chromium 自身崩溃」。**实测 journal 证明不是崩溃，是
systemd-logind 主动杀掉了整个浏览器。** 证据链：

```
16:07:23  sshd: session closed for user ubuntu        ← 最后一个登录会话登出
16:07:23  systemd-logind: Removed session 79.
16:07:33  systemd[1]: Stopping User Manager for UID 1000...      ← 整个用户管理器被停
16:07:33  systemd[5504]: Stopping app-org.chromium.Chromium-31673.scope...
16:07:36  Stopped app-org.chromium.Chromium-31673.scope.
16:07:40  run.log: 页面不存在/已关闭，正在重新打开…               ← 机器人开始瞎
17:40:31  run.log: 最后一条 BrowserContext.new_page ... closed（全程共 2488 条）
```

进程归属（决定性）：

```
Chromium      → /user.slice/user-1000.slice/user@1000.service/app.slice/app-org.chromium.<pid>.scope
机器人(pm2)    → /system.slice/pm2-ubuntu.service      ← 所以它毫发无伤、一直打「运行中」心跳
Xvfb          → /system.slice/xvfb.service
Linger = no                                            ← 关键开关
```

**机制**：`Linger=no` 时，ubuntu 的最后一个登录会话一结束，systemd 就停掉 `user@1000.service`，
**连带杀掉挂在它下面的 Chromium scope**。机器人本体在 pm2 系统服务里，于是「看起来正常、实际全瞎」
93 分钟。**这是必然复现的行为，不是偶发崩溃。**

**修复（第 1 步，已执行并验证）**：`sudo loginctl enable-linger ubuntu` → `Linger=yes`，
标记文件 `/var/lib/systemd/linger/ubuntu` 已落盘（重启后仍生效）。

**验证方法（可复现，非推测）**：主动把 logind 会话数清零（`loginctl terminate-session` 掉全部会话），
观察 journal：

```
19:03:43  Removed session 525.            ← 会话数 = 0
          ↓ 54 秒零会话窗口
19:04:37  New session 527.                ← 重连
判定：'Stopping User Manager for UID 1000' = 0 次；'chromium scope Stopping' = 0 次
结果：Chromium PID 84211 未变（存活 01:18:07）；机器人 PID 84196 未变；
      run.log 里「重开失败」计数 2488 → 2488（一条没新增）
对照 16:07 那次同样动作：10 秒内用户管理器被停 → 浏览器被杀 → 瞎 93 分钟。
```

### 0.2 B1 自愈代码（第 3 层，已写完并通过隔离自检，**待重启验证**）

即使触发器已消除，浏览器仍可能因 OOM / 自身崩溃 / 被误杀而整体死亡，所以这一层必须有：

| 位置 | 改动 |
|---|---|
| 新增 `CTX_DEAD_MARKS` / `ctx_dead_error()` | 区分「单个标签页被关」（重开即可）与「整个上下文已死」（重开必然失败） |
| 新增 `launch_persistent()` | 启动与自愈共用唯一入口，避免两处参数漂移 |
| 新增 `chrome_pids_of_profile()` / `kill_profile_chrome()` / `clear_singleton_locks()` | 重建前清理残留 chrome 与 profile 锁（否则新 context 会因 profile 被占用而启动失败） |
| `main()` 启动处 | 首次启动失败 → 只杀**孤儿** chrome（ppid==1）+ 清锁 → 重试一次（防崩溃循环） |
| `relaunch_browser()` | 关旧 ctx → 清残留 → 重建 persistent context → 重开全部页面 → 重预热 → **立即飞书告警**（含失明时长）；保留游标，积压走回补闸门；节流 120s 防崩溃循环 |
| 页面关闭分支 | 识别上下文死亡 → 累计 `_dead_groups`；**日志降噪**（事故时那两行刷了 2488 条，现在只记前 2 次 + 每 20 次） |
| 每轮开头 | 连续 2 轮命中 or 单轮 ≥2 个群命中 → 触发全量重启 |
| 轮询异常分支 | 原代码重建失败后 `pages[g]=None` **不记原因**；现已补日志 + 上下文死亡识别 |
| 新增 `--selftest-b1` | 隔离自检：生产路径全部重定向 `/tmp`，真起浏览器 → 杀掉 → 验证检出 → 验证重建 |

**自检结果（服务器实跑，rc=0 全通过）**：
```
[1] 异常分类 9/9 通过（含 2026-09-15 事故的真实报错串；普通超时/网络重置/元素歧义均不误判）
[2] 端到端：真起 /tmp 浏览器 → kill 掉 8 个 chrome 进程 → new_page 抛出
    「BrowserContext.new_page: Target page, context or browser has been closed」
    → 被正确识别为上下文已死 → 清掉 SingletonLock/Cookie/Socket → 重建成功 → evaluate=42
[3] 隔离复核：生产 run.log 行数不变、state.json/runtime_config.json mtime 未被改、
    生产日志里 'B1 自检'/'B1 自愈'/'b1_selftest' 各 0 条、生产 dryrun_bot2.py MD5 不变、
    测试浏览器进程全部清干净
```

### 0.3 本次新发现（原文档没有）

**B10 —— 英文常用词撞币种代码，把单币信号误判成「多币种总结」（已用代码 + 数据双向证实）**

`find_all_coins()` 用的是**大小写不敏感**的词边界正则：

```python
re.search(r"(?<![A-Za-z0-9])" + re.escape(s) + r"(?![A-Za-z0-9])", t, re.I)
```

而 `ONUSDT` / `ATUSDT` / `THEUSDT` 都是币安真实合约（已在 `fapi_symbols.json` 里核对），
于是句子里的 **`on` / `at` / `the`** 被当成币种。实测现场（19:03）：

```
原文（单个币）：Going long on UNI here at CMP. TPs above, 4H close under 6.39 for stops...
机器人反应：待确认池 ON 也建了一条；通知写「这条消息里出现了 4 个币种（AT、ON、THE、UNI）」
           并对 ON 发了【信号·拒绝】
```

**影响**：任何含 on/at/the 的英文信号都会**误判成多币种**→ 触发假的多币种审批分支，
这是 B3/B4 噪音的真正来源之一。**归入修复第 4 项与 B5 一起改**（改大小写敏感，或对
`ON/AT/THE/IN/SO` 之类歧义词加排除表）。

**B11 —— 待确认池的两个缺陷**

1. **只存在内存里**：`state.json` 里没有 ASKING（已核对键为 `last/open/risk/seen/ts`）
   → **机器人一重启，所有待确认单静默丢失**。
2. **坏的解析会覆盖好的解析**：`ask_user()` 里 `ASKING[coin] = {...}` 无条件覆盖。
   实测 UNI 19:03:22 的参数是好的（开仓=6.513 止损=6.396 止盈=[9.289]），
   19:03:45 被一条更差的覆盖成（开仓=None 止损=6.39 **止盈=[6.39]** —— 止盈=止损，自相矛盾）。

**B12 —— run.log 无轮转，心跳淹没日志**

2.23MB / 2.5 万行中 **66%（17071 行）是心跳**，`health_check.py` 的「最近 400 行」窗口被心跳占掉 246 行
→ **故障关键字会被挤出窗口导致漏报**。这是 B2 必须一起解决的前置问题。

**B13 —— 17:40 有测试脚本污染生产日志与风控状态（已核实未推飞书）**

```
17:40:14  ⏱ [测试群] NEW2 出单 | 市价开仓=100.0 信号价=None 止损=95.0 止盈=[110.0]
17:40:14  🛑 风控熔断：连续亏损 3 笔（上限 3）／今日已实现净亏损 -300.00U
17:40:14  ⚠️ 交易所有仓、纸面无记录（孤儿仓/手工仓）：SOLUSDT 数量 5
17:40:14  🚫 [对账闸门] 已阻止真实开仓（DOGE）
17:40:15  🎛 收到指令：重新对账
```
**核实结论**：这些行**都没有 `[通知]` 前缀**，而 `notify()` 一定会先打 `[通知]`；
17:39–17:47 区间内仅有的两条 `[通知]` 是 17:44 的正常启动与回补通报。
→ **该批告警没有推送飞书**，用户未收到假告警。但日志与内存风控计数被污染，
现 `state.json` 的 `risk` 已回到 `consec_loss:0 / day_pnl:0.0`。
**再次印证交接文档第十一节第 16 条：测试脚本必须把 `RUNTIME`/`TRADES` 等重定向到 `/tmp`。**

### 0.4 与原文档的其它偏差（据实修正）

| 原文档 | 实测 |
|---|---|
| 「最新提交 GitHub `5d84f33`」 | 实际 `ea80e4e`（本地 HEAD = origin/main = GitHub 三处一致） |
| 「`config.json` 含 binance 四段，`live_trading` 在里面」 | `config.json` 的 `binance` 段只有 `api_key/api_secret/note`；**`live_trading` 在 `runtime_config.json`**（影子模式结论正确，位置会误导人去改错文件） |
| 「现货/资金钱包 USDT 2229.61」 | **API 读不到**（该 key 的现货权限已关闭，对账脚本自报 `现货/杠杆 = False`）→ 需在币安 App 人工核对。合约钱包 0.00 为 API 实读，已核实 |
| 「健康检查仅报持仓 6>5」 | 一致；另实测内存可用 738MB/3720MB、swap 已用 456MB |

---

## 一、已确认的 bug / 缺口

### B1 —— 浏览器上下文死亡后无法自愈（最高优先级）

**真实事故证据**
```
16:00:26  🎛 收到指令：持仓情况            ← 最后一次正常
16:07:40  页面不存在/已关闭，正在重新打开…
16:07:51  页面不存在/已关闭，正在重新打开…   ← 每 3 秒重试，一直失败
17:19:17  重开失败：BrowserContext.new_page: Target page, context or browser has been closed
   ⋯ 心跳一直打「运行中 | 持仓 6 笔」       ← 看起来正常，实际完全读不到任何群
17:40:15  重启机器人 → 恢复
```

**后果**：机器人"瞎"了 **93 分钟**，期间四个群的消息**全部漏掉**；
用户 16:37 发的「只开btc和doge」、17:10 发的「持仓情况」「帮助」**根本没被读到** →
用户感知为"机器人指令没反应"。

**根因**：~~整个 `BrowserContext` 死亡。上一轮加的"页面重开失败继续重试"
**只覆盖标签页被关，覆盖不了上下文整体死亡** —— `ctx.new_page()` 必然失败。~~

> ⚠️ **本节根因已于 2026-09-15 晚修正，见上面第 0.1 节**：
> 不是 Chromium 崩溃，是 **systemd-logind 因 `Linger=no` 停掉 `user@1000` 时把 Chromium 连坐杀掉**。
> 触发器已用 `loginctl enable-linger ubuntu` 消除并验证；自愈代码见第 0.2 节。

**修法**：检测到 `Target page, context or browser has been closed`
→ **主动重启整个浏览器**（重新 `launch_persistent_context` + 重开所有页面 + 重新预热），并**立即告警**。
（已实现，见第 0.2 节，`--selftest-b1` 全通过，**待生产重启验证**）

**旁证**：~~内核日志**无 OOM 记录**，不是内存问题，更像 Chromium 自身崩溃。~~
→ **已修正**：确实无 OOM（本次用 `sudo dmesg`/`journalctl` 复核确认），但也不是 Chromium 崩溃，
是 **systemd 主动 stop 掉 `app-org.chromium.Chromium-<pid>.scope`**。

---

### B2 —— 失联看门狗阈值太长 + 检测维度不够

- 现状 `silence_alert_hours: 6` → **6 小时才报警，救不了 B1 这种 93 分钟的事故**
- 需：① 阈值收紧（建议 30 分钟，可配）② **检测"页面连续打不开 N 次"→ 立即告警**

**✅ 已发现低成本捷径**：`tools/health_check.py` 第 4 项**本来就会扫日志里的「失败/错误」行**，
实测能把 `BrowserContext.new_page: ... has been closed` 原样列出（32 条）。
→ 加 **cron / systemd timer，每 5 分钟跑一次 health_check**，
命中 `BrowserContext` / `页面不存在` / `重开失败` 或输出含「发现 N 个问题」→ **立刻推飞书告警**。
**不依赖机器人进程活着**，能同时兜住 B1 与 B2，改动极小，建议优先做。

---

### B3 —— 多币种分支绕过了合理性校验

`split_by_coin` → 直接 `ask_user`，**没经过 `finalize_pending` 那套校验**
（止损方向、价格合理性、点数换算）。

**证据**：DOGE 做多，入场 0.079、**止损 0.09（高于入场价）** ——
按已有校验**本该被拒绝**，但因走多币种分支直接发给用户审批了。

**修法**：把校验抽成公共函数，两条路径都过。

---

### B4 —— 多币种审批选项不完整

用户要求：3 个币应给出 **6 种组合**
（只开 A / 只开 B / 只开 C / A+B / A+C / B+C）。
现状只给「只开 A 和 B」「只开 A，不开 B」「全部不开」，且前两个语义重复。

---

### B5 —— 解析体系对"点数 / 条件单 / 语序"无能为力

**原始消息 → 实际解析（错误）对照**
```
原文：以太坊空单…在2465跌破可以追空，止损带个30点左右，止盈就30点以上分批止盈。
      比特币等待76000跌破收回，站稳76200多，74000止损，可以设置条件单…2000点以上的止盈利润。
      狗狗币在0.078到0.08接多，左侧轻仓，0.075止损，止盈0.09附近。

实际：ETH  做空 开仓=2465.0 止损=30.0  止盈=[30.0, 2465.0]   ← 完全错误
      BTC  做多 开仓=76200.0 止损=74000.0 止盈=未读到        ← 条件单，不该直接开
      DOGE 做多 开仓=0.079 止损=0.09 止盈=[0.09]            ← 止损错（0.09 是止盈）
```

| 子问题 | 说明 | 正确结果 |
|---|---|---|
| **点数 vs 价格** | 「止损带个30点左右」「止盈就30点以上」的 **30 是点数** | ETH 入场 2465 → 止损 **2495**、止盈 **2435** |
| **数字在关键词前** | 「0.075止损」现有正则要求 `止损` 在数字前，故漏读 | DOGE 止损 **0.075** |
| **条件单** | 「等待76000跌破收回，站稳76200多」「跌破2465可以追空」= 条件触发才进 | **必须送审批，不得自动开** |
| **止损止盈互混** | DOGE 把止盈 0.09 当成止损 | 必须交叉校验 |

---

### B6 —— 「持仓情况」缺来源群 + 信号时间

用户明确要求加：要能看到**这条单来自哪个群、依据是什么时间的消息**。

---

### B7 —— `clientOrderId` 幂等键 + 503 / 限频处理（用户已批准"开始"，未做）

- 币安 **503「执行状态未知」**时订单**可能已成交**；无幂等键则无法安全"查了再决定"，
  盲目重试会**下重单**
- 现状：**全项目 0 处 `clientOrderId`**、**无重试/退避**、**无 -1003 限频处理**

---

### B8 —— 进程死亡时的外部告警（用户已选腾讯云，未配）

机器人进程死了它自己发不出告警。需在**腾讯云控制台配告警**。
现有失联看门狗只覆盖"进程活着但收不到消息"。

---

### B9 —— 响应速度（**尚未分段落测量**）

用户反馈"响应还是太慢，特别是抓取监控信号这一步"。
已知总时长实测 **5.9s ~ 29.7s**，日志 `⏱` 行有分段（发现/抓图/解析/读图/等待/推送）。
**应先测量各段占比再优化**。怀疑点：`发现` 段（会话列表预览判定 + safety%30 兜底扫描）与串行阻塞。

---

### B10 ~ B13 —— 2026-09-15 晚新发现（详见上面第 0.3 节）

| 编号 | 问题 | 归属 |
|---|---|---|
| **B10** | **英文常用词撞币种代码**（`find_all_coins` 用 `re.I`，句子里的 `on`/`at`/`the` 撞上真实的 `ONUSDT`/`ATUSDT`/`THEUSDT`）→ 单币信号被误判成「多币种总结」→ 假审批/假拒绝 | 并入第 4 项与 B5 一起改 |
| **B11** | 待确认池 ① **只在内存里**（重启即静默丢失）② **坏解析会覆盖好解析**（`ask_user` 无条件覆盖 `ASKING[coin]`） | 第 3 项（审批闸门） |
| **B12** | `run.log` **无轮转**，66% 是心跳 → `health_check.py` 的 400 行窗口被挤占，**故障关键字会漏报** | 第 2 项（B2） |
| **B13** | 17:40 测试脚本污染生产日志与内存风控计数（**已核实未推飞书**）→ 再次踩了第十一节第 16 条 | 流程约束 |

---

## 二、建议的修复顺序

| 顺序 | 内容 | 依据 | 状态（09-15 晚） |
|---|---|---|---|
| **1** | B1 浏览器死亡自愈 + 立即告警 | 不修则其它一切被掩盖 | 🟡 **根因层已完成并验证**（`enable-linger`）；自愈代码已写 + 自检通过，**待重启验证** |
| **2** | B2 看门狗阈值收紧 + health_check 定时巡检告警（+ B12 日志轮转） | 成本极低、收益立竿见影 | 🟡 进行中 |
| **3** | 用户新要求：**所有订单开之前都经用户审批** + B4 六种组合（+ B11 待确认池健壮性）| 用户 09-15 明确要求 | ⬜ 未开始 |
| **4** | B5 解析体系 + B3 多币种分支补校验（+ **B10 词撞币种**）| 当日错单全出在这（**大改，可能拆 2~3 轮**）| ⬜ 未开始 |
| **5** | B6 持仓情况加来源群 + 信号时间 | 小 | ⬜ 未开始 |
| **6** | B7 幂等键 + 503 / 限频 | 中 | ⬜ 未开始 |
| **7** | B8 腾讯云告警 | 需用户在控制台操作 | ⬜ 未开始 |
| **8** | B9 先测量再优化响应速度 | 中 | ⬜ 未开始 |

---

## 三、相关设计原则（改代码时必须遵守）

1. **绝不猜价** —— 读不准就询问用户，不自动开单
2. **绝不静默丢弃** —— 任何解析失败/未识别信号都要留日志并通知
3. **默认不碰真钱** —— `binance_exec.LIVE[0] = False` 是默认值
4. **一切可核验** —— 金额、点位、成交、盈亏都要能从交易所流水/日志复核
5. **保命机制先挂后撤** —— 改止损必须"先挂新 → 确认成功 → 才撤旧"
6. **订单开之前必须经用户审批**（09-15 新增）
