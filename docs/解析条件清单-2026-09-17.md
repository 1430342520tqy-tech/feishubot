# 解析条件清单 —— 机器人现在到底按哪些规则判断"这是不是信号"、以及怎么从消息里取数字

> **这份文档的用途**：把所有"判断条件 / 关键词 / 正则 / 阈值"逐条摊开，方便你直接改规则。
> **范围**：只讲"收消息 → 判断是不是信号 → 从文字和图里取币种/方向/开仓/止损/止盈 → 校验 → 送审批"这条链。
> 不包含：下单执行细节、结算统计、看门狗策略。
>
> **代码基准**：`src/dryrun_bot2.py`（共约 6271 行，LF 换行；行号即此文件行号）、`src/feishu_api.py`（约 407 行）。
> 行号写法：`dryrun_bot2.py:4583` = 该文件第 4583 行。
>
> **几个必须知道的前提**
> 1. 本文件是**纯清单**，一切以代码为准；代码里"注释说的"和"实际做的"不一致的地方，我单独标了 **【现状】**。
> 2. 有几个文件**不在仓库里**（在生产服务器上），所以里面的具体内容我无法核对，只能告诉你它管什么：
>    - `fapi_symbols.json`（`BASE + "/fapi_symbols.json"`，`dryrun_bot2.py:1349`）：币安 USDT-M 合约清单（约 897 个），决定"这个币能不能交易"。**你改不了它的规则，但它的存在与否决定币种校验是否生效。**
>    - `runtime_config.json`（`dryrun_bot2.py:2925`）：保证金、杠杆、监控群、持仓上限、`require_approval`、`ai_first_parse`、`strict_limit_groups`、`live_trading`、`fetch_mode`、`loss_limit_pct`、`silence_alert_hours`、`max_total_margin`、`daily_loss_limit`、`max_consec_loss`。**这份清单里所有"默认值"都可能被它覆盖。**
>    - `config.json`（`dryrun_bot2.py:29`）：DeepSeek key、飞书 app、binance key。
>    - `notify.json`（`dryrun_bot2.py:23`）：飞书 webhook 地址。
> 3. 每条信号最终**必须经你回复「开」**才会开单（`REQUIRE_APPROVAL` 默认 `True`）；所以下面很多"会被拒绝/会问你"的分支，实际表现都是"变成一条问你确认的通知"。
> 4. **"当前规则"这一列是字面量**，我尽量原样抄（含大小写、标点、空格）。

---

## 目录

- [1. 消息层门槛：哪些消息会被处理/跳过](#1-消息层门槛哪些消息会被处理跳过)
- [2. 币种识别](#2-币种识别)
- [3. 方向识别（做多/做空）](#3-方向识别做多做空)
- [4. 数字提取（最重要）](#4-数字提取最重要)
- [5. 中英文关键词总表 + 目前【不支持】的写法](#5-中英文关键词总表--目前不支持的写法)
- [6. AI 解析](#6-ai-解析)
- [7. 校验与拒绝](#7-校验与拒绝)
- [8. 多币种](#8-多币种)
- [9. 读图（K 线图上的线/框/标签）](#9-读图k-线图上的线框标签)
- [10. 审批与下单前的固定数字](#10-审批与下单前的固定数字)
- [附录 A：已知缺陷 / 待确认（现状汇总）](#附录-a已知缺陷--待确认现状汇总)
- [附录 B：一条消息的完整判定顺序](#附录-b一条消息的完整判定顺序)
- [附录 C：我**不确定**的地方](#附录-c我不确定的地方)

---

## 1. 消息层门槛：哪些消息会被处理/跳过

判定顺序：**取消息 → 去重(SEEN) → 自环过滤(SELF_MARKS) → "自己发的 app 消息"过滤 → 指令(仅指令群) → 关键词/图片门槛 → 结单通报拦截 → 解析**。

### 1.1 门禁类

| 项目 | 当前规则或取值 | 位置 | 作用 | 举例 |
|---|---|---|---|---|
| 监控群（默认值） | `GROUPS = ["机器人开单通知", "暴富龙", "UA-nurseneil2", "颜驰2群"]` | `dryrun_bot2.py:39` | 只扫这些群 | 【动态】生产实际值在 `runtime_config.json` 的 `groups`（`dryrun_bot2.py:2992-2993` 覆盖），仓库里看不到 |
| 取消息方式 | `FETCH_MODE = "api"`（可改成 `browser`） | `dryrun_bot2.py:139`、`2995-2996` | 官方 API / 爬网页 | 【动态】`runtime_config.json` 的 `fetch_mode` |
| 轮询频率（API） | 每个群最多 **2 秒**轮询一次 | `dryrun_bot2.py:4467-4468` | 控制接口频率 | 4 个群 ≈ 120 次/分钟 |
| 轮询频率（浏览器兜底） | `POLL_SEC = 0.5` 秒/轮 | `dryrun_bot2.py:40`、`5099` | 主循环节拍 | 每 15 轮（≈15–30s）全量扫一次兜底：`safety % 30 == 0`（`4395`） |
| 消息去重 | `SEEN` 集合，最多留 `SEEN_MAX = 1500` 条 | `dryrun_bot2.py:2931-2932`、`2934-2941`、`4544-4549` | 防重启后重复开单 | 命中则日志"该消息此前已处理过，跳过" |
| 取消息去重（更早一层） | `fetch_new(..., skip_ids=SEEN)` 在**下载图片之前**就跳过 | `feishu_api.py:377-378`、`dryrun_bot2.py:4479` | 避免重复下载图 | — |
| API 回看窗口 | 从游标往前多取 **5 秒**：`since_s = int(since_ms/1000) - 5` | `feishu_api.py:364` | 防边界丢消息 | — |
| 单次最多取多少条 | `max_msgs=60` | `feishu_api.py:341`、`369` | 每次轮询上限 | — |
| 首次使用 API 的起点 | 无游标的群从 **10 分钟前**起算：`int((time.time()-600)*1000)` | `dryrun_bot2.py:4181-4184` | 不回补更早历史 | 生产首次上线用 |
| 正文长度（爬网页） | `text` 截断到 **1200 字符** | `dryrun_bot2.py:3694`（`SCAN_JS`） | 送进解析的文本上限 | — |
| 正文长度（送 AI） | 只送前 **900 字符**：`text[:900]` | `dryrun_bot2.py:1249` | AI 输入上限 | 长文的尾巴永远看不到 |
| 合并保存的原文 | 每条存前 **400 字符**，最多 **6 条** | `dryrun_bot2.py:1691-1694` | 待确认池里的原文 | — |
| 发送者名前缀剥离 | 4 条规则，见下 | `dryrun_bot2.py:2193-2208` | 防止"BOT"被当成币种 | 见 1.4 |

### 1.2 自环过滤（机器人不能把自己的通知当信号）

| 项目 | 当前规则或取值 | 位置 | 作用 |
|---|---|---|---|
| 自环前缀表 `SELF_MARKS` | 见下面代码块（共 **32** 条） | 定义：`dryrun_bot2.py:1030-1052`；使用：`4561` | 消息正文含任一条 → 直接 `continue` |
| 补充过滤（非前缀表） | 正文含 `通过webhook`、或 lower 后含 `invited` / `test notification`、或含 `（你的指令：` / `本次盈亏：` / `该档盈亏：` / `累计：` | `dryrun_bot2.py:4561-4565` | 同上 |
| "自己发的 app 消息" | `_is_self_app_row(g, sender_type)`：`sender_type` 小写等于 `"app"` **且** 群在 `CMD_GROUPS` 里 → 跳过 | `dryrun_bot2.py:3217-3240`；使用：`4573-4575` | 关掉"机器人读回自己通知"的入口；拿不到 `sender_type`（浏览器模式）一律返回 False（不误杀） |
| 【现状】为什么不能一刀切 | KOL 群里的博主信号 `sender_type` **也是 "app"**，且 app_id 与自己的 webhook 相同（实测注释 `feishu_api.py:350-359`、`dryrun_bot2.py:3220-3225`） | 同上 | 所以只对 `CMD_GROUPS` 生效 |

`SELF_MARKS` 全部条目（`dryrun_bot2.py:1035-1049`，照抄）：

```python
["【跟单机器人】", "【机器人指令】", "【已开单·纸面】",
 "【已结单·纸面】", "【止盈成交·纸面】", "【已平仓·纸面】",
 "【已减仓·纸面】", "【你的持仓】", "【指令】", "【博主指令】",
 "【信号·", "【待确认】", "【挂单情况】", "【机器人状态】",
 "【持仓情况】", "【风控熔断", "【对账闸门】", "【持仓自检】",
 "【手工仓护栏】", "【机器人告警】",
 "【止盈更新·纸面】", "【实盘·成交】", "【实盘·成交但要你处理】", "【实盘·未成交】",
 "【实盘·看门狗】", "【实盘·部分成交】", "【实盘动作失败·需要处理】",
 "【失联看门狗】", "【启动对账失败】",
 "【取消息】", "【取消息告警】",
 "【指令·没看懂】"]
```

> **规则要求（代码里的自检）**：源码里任何 `notify("【…】…")` 用到的新前缀**都必须登记进这张表**，否则机器人会把自己的通知当信号重解析（自检在 `dryrun_bot2.py:5881`、`5894-5900`）。你新增通知时别忘了加。

### 1.3 指令群 / 审批回复

| 项目 | 当前规则或取值 | 位置 | 作用 |
|---|---|---|---|
| 指令群 | `CMD_GROUPS = ["开单记录", "机器人开单通知"]` | `dryrun_bot2.py:2926` | 只有这两个群里才把消息当**指令**；`_chitchat_hint` 也只在指令群生效（`3253`、`4577`） |
| 指令长度 | `len(t) > 60` → 不当作指令 | `dryrun_bot2.py:3376-3377` | 防止转发内容被误当指令 |
| 指令关键词 `KEY` | `["帮助", "状态", "持仓情况", "持仓", "全部平仓", "确认全部平仓", "平仓", "减仓", "修改止损", "移保本", "暂停", "继续", "修改监控群", "修改金额", "修改杠杆", "测试模式", "实盘模式", "挂单情况", "挂单", "待确认", "修改持仓上限", "持仓上限", "进入测试模式", "进入实盘模式", "重新对账", "对账", "解除手工仓", "手工仓已平"]` | `dryrun_bot2.py:3378-3381` | 命中方式：消息**开头**必须是其中之一（先后去掉时间/昵称前缀，`3403-3406`）；若整条 ≤12 字，则允许关键词出现在任意位置（`3413-3416`） |
| 审批回复识别 | `_is_ask_reply`：候选文本先去掉时间前缀、昵称前缀、首尾标点/emoji；长度 **>48 字**跳过；命中条件 =（**有币种** 且 含 `开|买|作废|不要`）或（整条完全等于 `_REPLY_WORD`） | `dryrun_bot2.py:3191-3214` | 决定"这条是不是你在回答审批" |
| `_REPLY_WORD`（整条匹配表） | `(开|开仓|开单|开吧|可以开|确认开|确认|下单|建仓|买入|买|执行|不开|别开|不要|作废|全部开|全开|都开|全部不开|全不开|都不开|都不要|全部作废)` | `dryrun_bot2.py:3181-3182` | 用 `re.fullmatch` |
| "全部开"判定 | 含 `全部开\|全开\|都开\|全买\|都买` 且**句中没有币种** | `dryrun_bot2.py:3291` | — |
| "全部不开"判定 | 含 `全部不开\|全不开\|都不开\|都不要\|全部作废\|都作废` | `dryrun_bot2.py:3282` | 作废全部待确认 |
| "只开"判定 | 含 `只开\|只买\|只要\|仅开\|仅买` | `dryrun_bot2.py:3298` | 后面的币种清单=只开这些 |
| 排除语义 | 含 `不开\|不要\|别开\|作废`；先按 `re.split(r"不开\|不要\|别开\|作废", s, maxsplit=1)` 切开，前半=要开的、后半=不开的 | `dryrun_bot2.py:3299-3308` | 【现状】空列表时**先看是不是排除语义**，是则一律作废、绝不开仓（`3321-3329`，修的是 2026-09-17「不开被判成开」事故） |
| 指令群里"没认出来"的兜底回话 | 群在 `CMD_GROUPS`、`sender_type == "user"`、长度 ≤40、且含 `开\|关\|仓\|单\|平\|止\|损\|盈\|确认\|模式\|持仓\|状态\|帮助\|撤\|停\|继续\|对账\|手工仓\|金额\|杠杆\|上限\|监控` → 回一句"【指令·没看懂】" | `dryrun_bot2.py:3246-3269` | 20 秒内只提示一次（`3262`） |

指令的参数抽取正则（你自己发指令时用得到，`dryrun_bot2.py`）：

| 指令 | 抽取规则 | 行 |
|---|---|---|
| 持仓情况 | `(?:持仓情况\|持仓)\s*([A-Za-z0-9]{2,12})` | `3474` |
| 平仓 | `平仓\s*([A-Za-z0-9]{2,12})` | `3541` |
| 减仓 | `减仓\s*([A-Za-z0-9]{2,12})(?:\s*(\d{1,3}))?`（没写百分比=50%） | `3548-3551` |
| 修改止损 | `修改止损\s*([A-Za-z0-9]{2,12})\s*([0-9]*\.?[0-9]+)` | `3556` |
| 移保本 | `移保本\s*([A-Za-z0-9]{2,12})`（止损→开仓价） | `3568-3573` |
| 修改监控群 | `修改监控群\s*(.+)`，按 `[,，、\s]+` 切 | `3587-3589` |
| 修改金额 / 杠杆 | `修改金额\s*(\d+)` / `修改杠杆\s*(\d+)` | `3597`、`3603` |
| 修改持仓上限 | `(?:修改)?持仓上限\s*(\d+)`，钳制到 `max(1, min(50, n))` | `3608-3611` |
| 解除手工仓 | 按 `[\s,，、]+` 切币种；写"全部"或没写币种=全部解除 | `3450-3452` |
| 实盘开关（最高危） | 含 `不\|别\|取消\|勿` 视为否定；`关`且非`不关\|别关` → 影子；`开`+`确认`且无否定 → 实盘 | `3642-3650` |
| 测试模式开关 | `关\|off` 与 `开\|on` 必须二选一，含糊就只回提示、不动配置 | `3658-3667` |

### 1.4 发送者前缀剥离 `strip_sender_prefix`（解析前必做）

| 顺序 | 规则（字面量） | 位置 | 作用 |
|---|---|---|---|
| ① | 去掉行首 `^\s*自定义机器人\s*BOT\s*` | `2193-2200` | "BOT"是币安真实交易对，会把单币消息误判成多币种 |
| ② | 去掉行首 `^\s*通过webhook将自定义服务的消息推送至飞书\s*` | `2202` | 转发前缀 |
| ③ | 去掉行首时间 `^\s*\d{1,2}:\d{2}\s*(AM\|PM)?\s*` | `2204` | — |
| ④ | 去掉 `^[^\s]{2,16}\s+(?=\d{1,2}:\d{2}\s*(AM\|PM)?)`（昵称+时间） | `2205` | — |
| ⑤ | 再去一次行首时间 | `2207` | — |
| 另：指令路径专用 | `_strip_nick` = `^[^\s]{2,16}\s+`；`_strip_time` = `^\d{1,2}:\d{2}\s*(AM\|PM)?\s*` | `3172-3179` | 飞书对同一发送者的连续消息会省略时间戳，昵称会留在正文里 |

### 1.5 关键词门槛 `SIG_KW` 与图片门槛

| 项目 | 当前规则或取值 | 位置 | 作用 |
|---|---|---|---|
| 信号关键词表 `SIG_KW` | `["long", "Long", "LONG", "short", "Short", "SHORT", "Entry", "CMP", "做多", "做空", "止损", "止盈", "平仓", "减仓", "close", "Closed", "TP", "SL"]` | `dryrun_bot2.py:4583-4584` | **局部变量**（写在 `main()` 里，不是模块级）；判定 = `any(k in txt for k in SIG_KW)`（区分大小写的**子串**包含） |
| 图片判定 | `msg_has_image(row)`：`row["nimg"] > 0` 就算"有图"（**不要求已加载**） | `dryrun_bot2.py:1729-1738`；使用：`4592` | 有图就走抓图/读图流程 |
| 跳过条件 | `not _has_kw and not has_img` → 日志"闲聊/无关，跳过" + 指令群兜底回话 | `dryrun_bot2.py:4593-4598` | 这就是"闲聊判定"的全部内容（**没有**独立的闲聊词表） |
| 抓图预算（浏览器模式） | `img_wait_ms`：`(nimg >= 2 or nblob > 0 or not has_kw)` → **6000ms**，否则 **1200ms** | `dryrun_bot2.py:1740-1748` | 只在图加载不出来时才真的等满 |
| 抓图判定（浏览器模式） | 只取 `naturalWidth >= 150` 且 `src` 以 `blob:` 开头的图 | `dryrun_bot2.py:3699-3718`（`FETCH_IMG_JS`） | 头像是 https，不会被当内容图 |
| 抓图（API 模式） | 直接下载原图，一条消息最多 **4 张**（`keys[:4]`） | `feishu_api.py:380-385` | 实测与网页 blob 字节相同 |
| 只读第一张图 | 读线/框只用 `imgs[0]` | `dryrun_bot2.py:4726` | 多图时后面的图不参与价位识别 |
| 读图缓存 | 按文件 sha1 缓存，同一张图不重复调 AI | `dryrun_bot2.py:2790-2807` | — |

> **【现状·重要】门槛比解析表窄**：`SIG_KW` 里**没有** `Stop`、`Target`、`目标位`、`入场`、`进场`、`加仓`、`限价`、`多单`、`空单`、`Buy`、`Sell`。
> 也就是说：一条只写「BTC 入场 77000，目标位 78000，Stop 76000」的**纯文字**消息，会在门口被判"闲聊/无关"直接跳过（日志 `↳ 闲聊/无关，跳过`），**根本走不到解析**。
> 想让这类消息能进来，就得往 `SIG_KW` 里加词（`dryrun_bot2.py:4583`）。

### 1.6 回补闸门（停机重启后不追过期单）

| 项目 | 当前规则或取值 | 位置 | 作用 | 举例 |
|---|---|---|---|---|
| 时效闸门 | `CATCHUP_MAX_AGE = 1800` 秒（30 分钟）：`now - t_sig > 1800` → 只通报不下单 | 常量 `55`；判定 `4516-4518` | 防拿过期点位追单 | 停机 2 小时后重启 |
| 数量闸门 | `CATCHUP_MAX_MSGS = 50`：单次扫描超过 50 条 → 只处理**最新** 50 条，其余只通报 | 常量 `56`；判定 `4521-4523` | 防一次性灌进太多老信号 | — |
| 被拦后仍推进游标 + 标记已处理 | `mark_seen(mid)`；API 模式**不动**网页游标 | `4524-4531` | 永不重复、也不静默丢弃 | 汇总通报见 `4912-4923` |
| 消息发出时间怎么算 | API 行用 `t_sig`（秒）；网页行用 `id >> 32` | `146-153` | 上面那条时效判定就用它 | — |

### 1.7 合并窗口 / 待确认池 / 图暂存区

| 项目 | 当前规则或取值 | 位置 | 作用 |
|---|---|---|---|
| 待确认合并窗口 | `PENDING_WAIT = 4` 秒 | `1589` | 同一条信号的后续消息（卡片/图）在这 4 秒内并入同一条 |
| 提前出单 | 信息齐全（`pending_complete`）且距首条消息 ≥ **1.5 秒** → 立即出单、不空等 4 秒 | `2393-2398` | `pending_complete`（`1707-1709`）= 开仓价有 且 止损有 且 去重后止盈 ≥ `TP_TIERS`(3) 档 |
| 图暂存区窗口 | `RAWQ_WAIT = PENDING_WAIT`（4 秒） | `1727` | 图先到、文字后到时的关联窗口 |
| 图暂存保留 | `RAWQ_TTL = 90` 秒、每群最多 `RAWQ_MAX = 20` 条 | `1725-1726`、`1759` | 清理用 |
| 文字先到、图后到 | 候选条件：同群、还没图、距首条 ≤ `RAWQ_WAIT + 1.5` 秒、且 deadline 未过；取**最后一个**候选 | `1805-1828` | 反向顺序也覆盖 |
| 图币种与文字不一致 | 只把图留档，**不用图上的线覆盖文字点位**（`_mc != coin` 分支） | `1795-1798`、`1823-1824` | 防串币 |
| 4 秒内没关联上 | 单独推送"【信号·只识别到图】"（**建不了可审批单**） | `1833-1886`、`4840-4857` | 用户要求③ |
| 待确认池评分（防更差覆盖更好） | `_plan_score`：开仓价 +3 ｜ 分批 legs +3 ｜ 止损 +2 ｜ 止盈每档 +1（最多 +3） | `2071-2083`；比较在 `2341-2349` | 新解析更差 → 不覆盖、不重新打扰你 |
| 待确认超时 | `ASK_TIMEOUT = 1800` 秒（30 分钟）自动作废 | `1924`、`2371-2379` | — |
| 暂停 | `PAUSED`：暂停时信号只记录不开单 | `2390-2392`；指令 `3577-3585` | 状态落盘，重启后仍暂停 |

### 1.8 结单/止盈通报的拦截（在拆币之前）

| 项目 | 当前规则或取值 | 位置 | 作用 |
|---|---|---|---|
| 结单通报总表 `_CLOSE_ANNOUNCE` | 见下面代码块 | `1904-1908` | 命中 → **绝不开新仓**，直接按下面规则处理 |
| 命中后的分支 | 若"只含止盈达成、无平仓字样"→ 当作**持仓回报**（`pos_report`）；否则**直接忽略、不推送** | `4626-4640` | 用户 09-16 要求："这是触发了止损，博主说一声，不是开单信号" |
| "只含止盈达成"判定 | 含 `TP\s?\d?\s*(hit\|nailed\|done\|reached\|filled)` 或 `take[- ]?profit\s*(hit\|reached)` 或 `止盈.{0,6}(达成\|到了\|命中\|触发\|已到)`，**且不含** `stop[ -]?loss` / `stopped\s+out` / `止损` / `平仓` / `closed` / `结单` | `4628-4631` | — |
| 位置 | 这一段在 `split_by_coin` **之前**执行（`4645`） | `4622-4640` | 任何分支都无法把它推成"博主指令" |
| 博主管理指令（另一道） | `INFO_ONLY` / `CLOSE_REQ` 两条正则：含"止盈达成"且无平仓字样 → 判定为通报、不动作，改为回报你的持仓 | `4800-4809` | 术语中文映射 `_ACT_CN = {"close_all": "全部平仓", "trim": "减仓", "move_stop_to_cost": "止损移到开仓价", "other": "其它"}`（`4796-4797`） |

`_CLOSE_ANNOUNCE` 原文（`dryrun_bot2.py:1904-1908`，`re.I`）：

```python
re.compile(r"(trade\s+closed|stop[ -]?loss\s+hit|sl\s+hit|stopped\s+out|tp\s?\d?\s*hit|"
           r"take[ -]?profit\s+hit|target\s+hit|hit\s+at|closed\s+at|profit\s+taken|"
           r"止盈(达成|已到|命中|触发|到位)|止损(达成|已到|被扫|触发|到位)|"
           r"已平仓|平仓完成|结单|全部平仓|已止盈|已止损)", re.I)
```

`INFO_ONLY` / `CLOSE_REQ`（`dryrun_bot2.py:4800-4801`）：

```python
INFO_ONLY = r"(TP\s?\d?\s*(hit|nailed|done|reached|filled)|take[- ]?profit\s*(hit|reached)|breakeven\s*hit|止盈.{0,6}(达成|到了|命中|触发|已到)|保本.{0,4}(止损|离场))"
CLOSE_REQ = r"(\bout of\b|\bclosed?\b|closing\b|exit(ing)?\b|stopped out|stop(ped)? (me )?out|fully closed|平仓|清仓|全部走|先走|离场|走人)"
```

### 1.9 数字与阈值汇总（消息层相关）

| 常量 | 取值 | 位置 | 作用 |
|---|---|---|---|
| `UNIDENT_NOTIFY_COOLDOWN` | `600` 秒 | `1898` | "【信号·未能识别】"每个群最多 10 分钟提醒一次 |
| `SILENCE_ALERT_H` | `6.0` 小时 | `350` | 失联看门狗阈值（`353-366`） |
| `FILL_TIMEOUT` | `1800` 秒 | `203` | 实盘限价单 30 分钟没成交 → 撤单 |
| `MAX_OPEN` | `5` 笔 | `44` | 持仓上限（【动态】`runtime_config.json` 的 `max_open`） |
| 达上限的额外询问 | 达上限且不在持仓里 → 回「开」= 把上限提高到 `len(open_pos)+1` | `2629-2631`、`3159-3162` | — |

---

## 2. 币种识别

### 2.1 归一化与"能不能交易"

| 项目 | 当前规则或取值 | 位置 | 作用 | 举例 |
|---|---|---|---|---|
| 后缀剥离 | 依次尝试去掉 `USDT.P`、`USDTM`、`PERP`、`USDT`、`USD`（取第一个命中） | `1328-1337` | 统一成币安 USDT-M 的 base | `BTCUSDT.P` → `BTC` |
| 清洗字符 | 转大写并去掉 `/`、`-`、空格、`$` | `1332` | — | `btc/usdt` → `BTCUSDT` → `BTC` |
| 别名表 `_COIN_ALIAS` | `GOLD→XAU`、`GOLDUSDT→XAU`、`XAUT→XAUT`、`SILVER→XAG`、`OIL→CL`、`WTI→CL`、`CRUDE→CL`、`USOIL→CL`、`NATGAS→NATGAS`、`GAS→NATGAS` | `1320-1326` | 俗称→合约 | `原油` → `CL` |
| 支持币校验 | `resolve_coin`：`b in _SYMS`（`_SYMS` 为空时**一律放行**） | `1339-1344` | 决定"币安 USDT-M 有没有这个币" | 不在清单 → 通知"【信号·不支持】…"并**跳过整条**（`4771-4777`） |
| `_SYMS` 来源 | `json.load(open(BASE + "/fapi_symbols.json"))`，只收以 `USDT` 结尾的，去掉后缀 | `1347-1355` | 【动态·仓库外】读不到就 `_SYMS = set()` | 交接文档说约 897 个合约 |
| 【现状】`_SYMS` 读不到时的后果 | `(not _SYMS) or (b in _SYMS)` → 校验恒真的"放行"；同时 `find_coin_in_text` 的代码兜底失效 | `1344`、`2884-2889` | 币种清单文件丢失时，错币不会被拦 | 需人工确认 |

### 2.2 `find_all_coins`（这条消息里出现了哪几个币）—— 三部分

| 顺序 | 规则 | 位置 | 说明 |
|---|---|---|---|
| ① 中文/俗称别名 | 遍历 `_NAME_MAP`，`len(k) >= 2 and k in t`（**区分大小写的子串包含**，`k` 是原样、没有转大写） | `2279-2281` | 命中就加入结果 |
| ② 第一遍：区分大小写 | 对每个 `_SYMS` 里的 `s`：要求 `len(s) >= 2` 且不是纯数字，且 `(?<![A-Za-z0-9])s(?![A-Za-z0-9])` 在**原文**里出现 | `2283-2286` | 杀掉英文句子里小写的 `on`/`at`/`the` |
| ③ 第二遍：大小写不敏感 | 要求 `len(s) >= 3`、不是纯数字、**不在 `_AMBIG_TICKERS`**，且大小写不敏感匹配边界 | `2288-2292` | 让 `btc`/`sol` 这种小写写法仍认得出来 |

> 判定多币种的地方：`split_by_coin`（`2296-2313`）与 `finalize_pending` 里的"笼统总结"检查（`2566-2569`，只看 `p["texts"][0]`，≥2 个币 → 送审批）。

`_AMBIG_TICKERS` 全部条目（`dryrun_bot2.py:1372-1381`，**共 93 个**，第二遍匹配时跳过；写成**大写**时第一遍仍会认出）:

```python
"ON", "AT", "THE", "IN", "SO", "ONE", "TWO", "FOR", "AND", "NOT", "NOW", "TOP",
"NEW", "OLD", "BIG", "MAX", "MIN", "KEY", "ALL", "ANY", "OUT", "UP", "DOWN",
"IF", "IS", "IT", "BE", "TO", "OF", "MY", "WE", "HE", "DO", "NO", "BY", "OR",
"AS", "AN", "ME", "US", "BUT", "CAN", "GET", "GOT", "LET", "PUT", "RUN", "SAY",
"SEE", "SET", "TOO", "USE", "WAY", "WHO", "WHY", "YES", "YET", "HIGH", "LOW",
"NEAR", "LINK", "SAND", "MASK", "GALA", "APE", "RUNE", "FLOW", "BAND", "STORJ",
"DOT", "KSM", "ICP", "SSV", "ID", "AI", "GO", "NFT", "DAO", "LONG", "SHORT",
"CLOSE", "OPEN", "STOP", "ENTRY", "TARGETS", "RISK", "PNL", "CMP", "SL", "TP",
```

`_GOING_LONG_SKIP`（`dryrun_bot2.py:1363-1367`，**只用在**「Going long/short **X**」模板里那个 X 位置，共 36 个）：

```python
"ON", "AT", "THE", "IN", "INTO", "TO", "OF", "AND", "OR", "FOR", "AS", "BY", "WITH",
"HERE", "NOW", "THIS", "THAT", "THEN", "IT", "IS", "BE", "ARE", "WAS", "WERE",
"SO", "ALL", "MY", "AN", "UP", "DOWN", "DO", "NO", "IF", "WE", "ME", "YOU",
```

用法：`for _tok in re.findall(r"\$?([A-Za-z0-9]{2,12})", m.group(2)): if _tok.upper() in _GOING_LONG_SKIP: continue; raw = _tok; break`（`1396-1400`）→ 跳过介词、继续往后找真正的币种词；全程跳过仍为空则走 `find_coin_in_text` 兜底（`1416-1420`）。

### 2.3 `_NAME_MAP`（中文/俗称 → 币安 base）全部条目

`dryrun_bot2.py:2856-2875`，照抄：

```python
_NAME_MAP = {
    # 主流币
    "比特币": "BTC", "大饼": "BTC", "BITCOIN": "BTC",
    "以太坊": "ETH", "以太": "ETH", "姨太": "ETH", "ETHEREUM": "ETH",
    "索拉纳": "SOL", "SOLANA": "SOL", "狗狗币": "DOGE", "狗币": "DOGE", "DOGECOIN": "DOGE",
    "瑞波": "XRP", "瑞波币": "XRP", "RIPPLE": "XRP", "币安币": "BNB", "艾达": "ADA", "艾达币": "ADA",
    "波卡": "DOT", "雪崩": "AVAX", "莱特币": "LTC", "柚子": "EOS", "波场": "TRX", "特朗普币": "TRUMP",
    # 贵金属 / 大宗
    "黄金": "XAU", "金": "XAU", "GOLD": "XAU", "XAUUSD": "XAU",
    "白银": "XAG", "银": "XAG", "SILVER": "XAG",
    "原油": "CL", "石油": "CL", "油": "CL", "OIL": "CL", "WTI": "CL", "CRUDE": "CL",
    "天然气": "NATGAS", "GAS": "NATGAS",
    # 美股代币（币安有对应 USDT 永续）
    "闪迪": "SNDK", "SANDISK": "SNDK", "海力士": "SKHY", "SK海力士": "SKHY", "SKHYNIX": "SKHY", "HYNIX": "SKHY",
    "特斯拉": "TSLA", "TESLA": "TSLA", "英伟达": "NVDA", "NVIDIA": "NVDA", "苹果": "AAPL", "APPLE": "AAPL",
    "微软": "MSFT", "MICROSOFT": "MSFT", "谷歌": "GOOGL", "GOOGLE": "GOOGL", "亚马逊": "AMZN", "AMAZON": "AMZN",
    "奈飞": "NFLX", "NETFLIX": "NFLX", "超微": "AMD", "英特尔": "INTC", "INTEL": "INTC", "美光": "MU",
    " coinbase": "COIN", "COINBASE": "COIN", "微策略": "MSTR", "策略": "MSTR", "帕兰提尔": "PLTR", "PLTR": "PLTR",
    "阿里": "BABA", "阿里巴巴": "BABA", "拼多多": "PDD", "游戏驿站": "GME", "标普": "SPY", "纳斯达克": "QQQ",
}
```

> **【现状·两个坑】**（都在 `dryrun_bot2.py:2856-2875`，改表时注意）
> - 键 `" coinbase"` **带一个前导空格**；因为 `find_coin_in_text` 里用了 `alias.strip()`（`2881`）所以仍能工作。
> - `"策略": "MSTR"`、`"金": "XAU"`、`"油": "CL"`、`"银": "XAG"` 是**单字/常用中文词**，正文里出现"策略""黄金价格"等口语时会被当成币种。

### 2.4 从整段文字兜底找币 `find_coin_in_text`

| 顺序 | 规则 | 位置 |
|---|---|---|
| ① | 把 `_NAME_MAP` 的键**按长度倒序**、`strip().upper()`，用**子串**（无词边界）在全文大写里找 | `2880-2883` |
| ② | 在 `_SYMS` 里找 `(?<![A-Z0-9])B(?![A-Z0-9])` 命中的，按币名**长度倒序**取第一个 | `2884-2889` |
| 返回 | 都没有 → `None` | `2890` |

### 2.5 只从图里认币种

| 项目 | 当前规则或取值 | 位置 | 作用 |
|---|---|---|---|
| 触发条件 | 文字里没解析出币种 **且** 有图 | `4781-4782` | 用**最后一张**图（`imgs[-1]`） |
| 判定 | `read_chart_meta` 返回 `is_chart == true` 且 `coin` 归一化后在币种清单里 → 采用 | `4783-4787`；`2809-2825` | 图上币种印在左上角（prompt 明说） |
| 方向兜底 | 图上没给方向时**默认 `"LONG"`** | `4786` | 【现状】默认做多 |
| 【现状】只识别到图 | 币种+方向都有、但文字没任何信号字段 → **不建可审批单**，只转暂存、4 秒后单独推送 | `4846-4857`、`1833-1868` | 用户明确要求"缺失信息绝不瞎猜" |

---

## 3. 方向识别（做多/做空）

方向来源两个：**本地正则 `fast_parse`** 与 **AI `direction` 字段**（AI 见第 6 节）。本地正则部分（`dryrun_bot2.py:1383-1566`）：

| 顺序 | 规则（字面量） | 位置 | 说明 |
|---|---|---|---|
| ① 英文模板 | `(?:Going\|Market\|Longing\|Buying\|Selling\|Shorting)\s+(long\|short)\s+(.{1,40})`（`re.I`）→ `LONG` if `long` else `SHORT` | `1392-1395` | 同时负责抓币种（X 位置，见 `_GOING_LONG_SKIP`） |
| ② 英文动词+币种 | `\b(Selling\|Buying\|Shorting\|Longing)\s+\$?([A-Za-z0-9]{2,12})`（`re.I`）→ `selling/shorting` = `SHORT`，否则 `LONG` | `1402-1405` | — |
| ③ 币-横线-方向 | `([A-Za-z0-9]{2,12})\s*(?:/USDT)?\s*[—\-–]\s*(LONG\|SHORT)\b`（`re.I`） | `1407-1409` | 例：`BTC — LONG` |
| ④ 中文做多词表（**覆盖**①②③的结果） | `区间多\|做多\|多单\|看多\|接多\|进多\|买多\|冲多\|追多\|低吸\|抄底` → `LONG` | `1412-1413` | 无条件赋值为 `LONG` |
| ⑤ 中文做空词表 | `区间空\|做空\|空单\|看空\|接空\|进空\|卖空\|高抛\|摸顶` → `SHORT` | `1414-1415` | 是 `elif`：**同一句里两个词表都命中时，永远判做多** |
| ⑥ 都没方向 | `dirc` 为空 → `fast_parse` 返回 `None`（交给 AI / 待确认） | `1430-1431` | — |
| 最终方向从哪来 | `p["dir"] = dirc`（单币路径 `4822`）；多币种路径 `_pp["dir"] = _dir_`（`4671`） | — | — |
| 出单时读方向 | `dirc0 = (p.get("dir") or "LONG").upper()` | `2401` | 【现状】方向缺失时**默认做多** |
| 审批文案里的方向 | `d = 1 if (p.get("dir") or "LONG").upper() == "LONG" else -1` | `2355` | 同上默认做多 |

> **【现状·注意】**
> - ④ 在 ①②③ 之后**无条件覆盖**：`Selling BTC 做多` 会被判成 `LONG`。
> - 两个中文词表都命中时按**做多**处理（`if/elif`）。
> - **多头/空头**这两个词**不在任何词表里**（"多单/空单"在，"多头/空头"不在）→ 只写"BTC 多头"的消息靠本地正则认不出方向。见第 5 节【不支持】。
> - `market`/`buy`/`sell` 单独的写法（如 `Buy BTC`）**不在方向词表**，而且 `Buy` 也不在 `SIG_KW` 门口表里 → 会先被当闲聊跳过。

---

## 4. 数字提取（最重要）

### 4.1 止损 stop

关键词表与正则（`dryrun_bot2.py:1436-1448`）。**按顺序**逐个试，**第一个命中**的就用（`break`）：

```python
_GAP = r"[^0-9，。；;、！!？?\n]{0,10}"
for pat in (r"close under\s*\$?([0-9]*\.?[0-9]+)",
            r"\bSL\s*[:：=]?\s*\$?([0-9]*\.?[0-9]+)",
            r"stop[ -]?loss\s*[:：=]?\s*\$?([0-9]*\.?[0-9]+)",
            r"\bstop\s*[:：=]?\s*\$?([0-9]*\.?[0-9]+)",
            r"止损\s*[:：=]?\s*" + _GAP + r"([0-9]*\.?[0-9]+)",
            r"([0-9]*\.?[0-9]+)\s*" + _GAP + r"(?:止损|stop)"):      # 「4250止损」
```

| 项目 | 当前规则或取值 | 位置 | 作用 | 举例 |
|---|---|---|---|---|
| 支持的写法 | `close under 6.39`；`SL: 0.1993`；`stop loss 6.39`；`stop 6.39`；`止损 4250`；`4250止损` | `1437-1442` | 取关键词**后**的数字，或**数字在关键词前**（最后一条） | `4H close under 6.39 for stops` → `6.39` |
| 间隔符 `_GAP` | `[^0-9，。；;、！!？?\n]{0,10}`：允许 ≤10 个**非数字、非标点**字符 | `1436` | 关键词和数字之间可以有"带个""大约"等修饰词，但**不许跨越标点** | `4250止损，4350到4450分批止盈` → 止损读到 4250（不是 4350） |
| 关键词在数字后 | 「数字+间隔+止损/stop」这一条支持（`1442`） | 同上 | 分批建仓那种"止损写在最后"的消息也能读 | `74000止损` |
| 【现状·`%` 会被跨越】 | `_GAP` 的字符类里**没有排除 `%`** → 「止损3%」「带个3%止损」会得到 `stop = 3` | `1436`、`1541`、`1658-1660` | 同时 `stopPct` 也 = 3（见 4.3）；由于 `p["stop"]` 已被填成 3，第 4.9 节的"百分比折算"分支（要求 stop 为空）不会触发 → 通常被"止损距离超过 40%"拦到审批（`2555-2556`） | 已用等价正则复算确认 |
| 【现状·无 `止损` 之外的写法】 | 没有 `Hard`、`Risk`、`支撑/阻力`、`Stop Loss %` 之类专门规则 | — | 见第 5 节 | — |

### 4.2 开仓 entry

| 顺序 | 规则（字面量） | 位置 | 举例 |
|---|---|---|---|
| ① 关键词在数字前 | `(?:Entry\|入场\|进场)[:：]?\s*\$?([0-9]*\.?[0-9]+)`（`re.I`） | `1455-1458` | `Entry 0.21144` → 0.21144；`入场77000` → 77000 |
| ② 数字在关键词前（"价格"写法） | `([0-9]*\.?[0-9]+)\s*价格?\s*(?:做多\|做空\|多单\|空单)` | `1461-1462` | `77065价格做空` → 77065 |
| ③ 现价/市价/CMP/@ | `(?:现价\|市价\|CMP\|@)\s*\$?([0-9]*\.?[0-9]+)`（`re.I`） | `1462` | `@77000`、`CMP 6.719` → 6.719 |
| ④ 区间开仓价 | 在**遮掉止盈段和止损段**的文本里找：`([0-9]*\.?[0-9]+)\s*(?:到\|至\|~\|～\|—\|–)\s*([0-9]*\.?[0-9]+)`；若没命中再试 `([0-9]*\.?[0-9]+)\s*-\s*([0-9]*\.?[0-9]+)\s*(?:区间\|之间)` | `1497-1510`（遮罩函数 `2211-2231`） | `在4360到4310区间多` → `entryRange=[4310,4360]`；最终取**中点并向上取整到合约精度**（`1619-1622`、`2918-2922`） |
| ⑤ 分批建仓 legs | 见 4.4 | — | — |
| **不支持**的写法 | 「数字在 `Entry`/`入场` 之后但中间夹了别的词」（如 `入场 位置 77000`）：`Entry` 那条正则中间**没有 `_GAP`**，必须紧跟 | `1455` | 【不支持】 |
| **不支持**的写法 | 「77000 入场」（数字在前、关键词在后，且不是"价格/做多"写法） | `1459-1468` | 【不支持】 |
| `entry_is_cmp` | `bool(re.search(r"\bCMP\b\|市价\|现价", txt, re.I))` | `1563` | 为真时的效果：出单时**忽略图上/文字价位，按当前市价入场**（`2416-2423`）；审批单会显示"按市价（CMP）"（`2095-2098`、`2114-2122`） |

### 4.3 止损的衍生规则（没给止损价时怎么办）

| 顺序 | 规则 | 位置 | 说明 |
|---|---|---|---|
| ① 百分比止损 | 正则 1：`(?:带个?\|带)?\s*([0-9]*\.?[0-9]+)\s*%\s*(?:的)?\s*止损`；命中不了再试 正则 2：`止损[^0-9%]{0,8}([0-9]*\.?[0-9]+)\s*%` | `1538-1546` | 只有 `0 < 值 < 90` 才收下（`1663`） |
| ② 百分比折算成价位 | `p["stop"] = entry * (1 - d0 * pct/100)`（`d0`：做多 +1、做空 -1），且**只在该仓还没有 stop 时**才折算 | `2467-2471` | 日志"百分比止损 3.00% → 止损位 …" |
| ③ 用第一止盈反推（2:1） | 没止损但有止盈：取**离入场最近**的那档，`stop = entry - d0 * |tp1-entry| / 2` | `2472-2479` | 保证 2:1 盈亏比 |
| ④ 两个都没有 | 只发提醒、不出单："没读到止损和止盈…等你确认" | `2480-2483` | 绝不猜价 |
| ⑤ 点数换算（`点数`≠价格） | 见 4.5 | — | — |
| ⑥ 止损区间 | 正则 `止损\s*[:：=]?\s*\$?([0-9]*\.?[0-9]+)\s*(?:到\|至\|~\|～\|-\|—\|–)\s*\$?([0-9]*\.?[0-9]+)` → 取**中点** | `1549-1552`、`1646-1656` | 【现状】区间必须**优先于**散点，代码里就是这么做的（注释 `1643-1645`） |

### 4.4 止盈 targets 与分批建仓 legs

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 止盈类的**全部关键词** `_BOUND` | `(?:止盈\|目标位?\|targets?\|(?:TP\|Tp\|tp)\s?\d?\|止损\|stop[ -]?loss\|SL\|加仓\|DCA\|入场\|进场\|Entry)` | `1474-1475`（`re.I`） | 这些词既用来"开一段"，也用来"结束上一段" |
| 扫描方式 | `re.finditer(_BOUND)` 得到**所有**关键词；只有**止盈类**的关键词才起点一段；每段 = 从该关键词结束到**下一个任意关键词**开始 | `1476-1481` | 逐个关键词扫描（不能只 `search` 第一个，否则漏第二档） |
| 段内取数 | 段内 `[0-9]*\.?[0-9]+` 全部取出 | `1482-1487` | 例：`止盈74500 78000` 会取两个数 |
| 档位序号剔除 | 若段内数字 ≥2 个**且**第一个是 `1.0/2.0/3.0/4.0` → **丢掉第一个** | `1488-1490` | `止盈1 0.24 0.26 0.28` → `[0.24,0.26,0.28]`（自检基准见 `5113-5116`） |
| 每段最多取 | 段内最多取 `TP_TIERS`(=3) 个 | `1491` | — |
| 去重与截断 | 按**出现顺序**去重（不排序！），整体再截到 `TP_TIERS` 档 | `1496` | 排序留到出单前按"离入场近→远"做（`2594-2602`） |
| 止盈区间 | `(?:止盈\|目标位?\|targets?)[^0-9]{0,10}\$?([0-9]*\.?[0-9]+)\s*(?:到\|至\|~\|～\|-\|—\|–)\s*\$?([0-9]*\.?[0-9]+)` → `targetRanges`，取**中点**入列；落在区间内的散点会被丢弃 | `1553-1557`、`1666-1690` | `止盈2400到2350` → 2375（一条，不是两条） |
| 档数 `TP_TIERS` | `3` | `45` | 全局：止盈最多 3 档 |
| 分批建仓 legs 正则 A | `((?:\$?[0-9]*\.?[0-9]+[\s,，、]+){1,4}\$?[0-9]*\.?[0-9]+)\s*(?:继续)?(?:分批\|分次\|分笔)` | `1518-1519` | `76666 75555继续分批接多` → `[76666,75555]` |
| 分批建仓 legs 正则 B | `(?:跌到\|涨到\|到)\s*\$?([0-9]*\.?[0-9]+)[^0-9]{0,12}?(?:第一笔\|第1笔\|首笔)[^0-9]{0,20}?\$?([0-9]*\.?[0-9]+)[^0-9]{0,12}?(?:第二笔\|第2笔)` | `1523-1524` | `跌到95第一笔，90第二笔` |
| legs 生效条件 | 去重后 **≥2 个点位**才收下；一旦有 legs → `entry` 置 `None`、来源记"分批建仓(N笔)" | `1527-1537`、`1629-1642` | 每笔保证金 = `MARGIN / N`（`2426`） |
| 加仓价 `add_price` | `(?:DCA\|Dca\|dca\|加仓)[^0-9]{0,14}\$?([0-9]*\.?[0-9]+)`（**无 `re.I`**，只认这三种大小写） | `1450-1453` | 【现状】这里的间隔用的是 `[^0-9]{0,14}`，**可以跨越标点**，与止损那条 `_GAP` 的修法不一致 → 可能吃到下一句的数字 |
| 【现状】`add_price` 的去向 | 只被存进 `p["add"]`（`1625-1627`）、传入 `fmt_plan(add=…)`（`2774`），而 `fmt_plan` 的形参 `add` **在函数体里从未被使用**（`1259-1317`） | `1259`、`1625-1627` | 也就是说：加仓价**解析了但从不显示、也从不参与下单** |
| `tp_on_chart` | `bool(re.search(r"TPs?\s+above\|止盈在?上方\|止盈位在上方", txt, re.I))` | `1565` | 【现状】只被存进 `p["tp_on_chart"]`（`1611-1612`），**全代码没有任何地方读它做判断**（只有反幻觉校验把它排除在外，`1945`）→ 目前**无实际作用** |

### 4.5 点数 vs 价格（`30点` / `3000点`）

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 判定正则 | `(?:止损\|止盈\|目标位?\|stop\|SL\|TP)[^0-9，。；;、！!？?\n]{0,10}([0-9]*\.?[0-9]+)\s*(?:个)?\s*点`（`re.I`） | `2249-2250` | 关键词和数字之间可以有 ≤10 个非数字非标点字符（如"带个""利润"） |
| 判据 | 只有当**原文里的这个数字**与待判定值**完全相等**时才算"这是点数" | `2252` | 用"点"字做确定性判据，不猜 |
| 换算（止损） | `new_stop = entry - d0 * 点数` | `2490-2496` | 做多：`entry - 点数` |
| 换算（止盈） | `new_tp = entry + d0 * 点数` | `2497-2508` | 做空反向 |
| 补漏 | 若整条只写了一个"止盈 N 点以上"而没进 tps：`(?:止盈\|目标位?)\s*[:：=]?\s*([0-9]*\.?[0-9]+)\s*点` → 换算成一档 | `2509-2518` | — |
| 换算后的强制动作 | 只要发生过点数换算 → **一律送人工审批**（绝不自动开） | `2583-2585` | 审批原因会写明"数字被识别为【点数】并已换算" |
| 多币种分支的同款原因 | `soft_ask_reason` 里也会给出"止损「N点」是点数不是价格…" | `4035-4045` | — |

### 4.6 条件单（要等价格条件满足才进）

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 条件词正则 `_COND_RE` | `等待\|等到\|跌破\|站稳\|收回\|站上\|突破\|破位\|若\|如果\|一旦\|触及\|达到\|回到\|确认后\|回调后\|回踩后\|等.{0,6}(?:再\|后)` | `2235-2236`；函数 `2259-2261` | 命中 → 这条消息**绝不自动开**，一律送审批（`2580-2582`） |
| 判定用文本 | 先过 `strip_sender_prefix` | `2261` | — |
| 审批原因文案 | "这条消息是【条件触发】型的（等跌破/站稳/突破/收回…才进），机器人不能替你盯价格条件 —— 得你自己判断现在能不能进" | `2581-2582` | — |

### 4.7 区间与百分比的其他处理

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 区间一律取中点 | 开仓区间（`1619-1622`）、止损区间（`1650-1655`）、止盈区间（`1670-1678`） | — | 开仓还会**向上取整到该合约 tickSize**（`2918-2922`、`2912-2916`） |
| tickSize 来源 | 币安 `GET https://fapi.binance.com/fapi/v1/exchangeInfo` 的 `PRICE_FILTER.tickSize`（全量拉一次并缓存） | `2893-2910` | 拉不到时退化为 `math.ceil` |
| 【现状】百分比止盈 | **没有**"止盈 N%" 规则：`止盈5%` 会被当作 `tps = [5]`（已用等价正则复算确认） | `1477-1496` | 后果：做多时 5 < 开仓价 → 被方向过滤剔除（`2541`）；做空时 5 会一路走到审批。到不了自动下单（所有单都要审批） |
| 遮罩函数 `_mask_segments` | 把"某类关键词之后、下一个任意关键词之前"的字符用**等长空格**替换（不替换换行） | `2211-2231` | 用来防止"止盈区间被当成开仓区间" |

### 4.8 `fast_parse` 什么时候返回 None（=交给 AI）

`dryrun_bot2.py:1558-1559`：`if not (stop or add or entry or tps or out_legs or out_stop_pct or out_stop_range or out_tp_ranges): return None`
也就是说：**止损/加仓/开仓/止盈（任一非空）都没读到 → 返回 None**。（注意：这一步在"legs / 百分比 / 区间"解析**之后**，所以只有止损写在数字后面、没有止盈关键词的消息（`76666 75555继续分批接多，74000止损`）不会被整体丢掉——注释 `1511-1514` 说明的就是这个坑。）

### 4.9 返回值字段一览（`fast_parse` 的返回结构，`1560-1566`）

| 字段 | 取值 | 位置 |
|---|---|---|
| `is_signal` | 恒 `True` | `1560` |
| `coin` / `direction` | 归一化后的 base / `LONG`\|`SHORT` | `1560` |
| `entry` / `entryRange` | 单一入场价 / 区间（**同时只有一个非空**） | `1560` |
| `entryLegs` | 分批点位列表或 `None` | `1561` |
| `stopPct` / `stopRange` / `stop` | 百分比止损 / 止损区间（待取中点）/ 单一止损价 | `1561`、`1564` |
| `targets` / `targetRanges` | 止盈档位 / 止盈区间（取中点后入 targets） | `1562`、`1564` |
| `targets[].tp_on_chart` | `TPs above` 等 → True（**无人使用**，见 4.4） | `1565` |
| `entry_is_cmp` | 见 4.2 | `1563` |
| `add_price` | 加仓价（**只存不显示、不使用**） | `1564` |
| `type` | 恒 `"open"` | `1566` |
| `_fast` | 恒 `True`（标记"这是本地正则的结果"） | `1566` |

---

## 5. 中英文关键词总表 + 目前【不支持】的写法

### 5.1 全部会出现的关键词字面量（按功能分组，"位置"是定义处）

| 功能 | 关键词字面量 | 位置 |
|---|---|---|
| **消息层门槛**（决定"要不要处理"） | `long` `Long` `LONG` `short` `Short` `SHORT` `Entry` `CMP` `做多` `做空` `止损` `止盈` `平仓` `减仓` `close` `Closed` `TP` `SL` | `4583-4584` |
| 方向·做多 | `区间多` `做多` `多单` `看多` `接多` `进多` `买多` `冲多` `追多` `低吸` `抄底` | `1412` |
| 方向·做空 | `区间空` `做空` `空单` `看空` `接空` `进空` `卖空` `高抛` `摸顶` | `1414` |
| 方向·英文模板 | `Going` `Market` `Longing` `Buying` `Selling` `Shorting` + `long`/`short`；`LONG`/`SHORT`（币-横线-方向） | `1392`、`1402`、`1407` |
| 止损（关键词） | `close under` `SL` `stop loss` `stop` `止损` | `1437-1442` |
| 加仓（关键词） | `DCA` `Dca` `dca` `加仓` | `1450` |
| 开仓（关键词） | `Entry` `入场` `进场`；`价格`（数字在前时）；`现价` `市价` `CMP` `@` | `1455`、`1461-1462` |
| 止盈（关键词 `_BOUND`） | `止盈` `目标位?`（=目标/目标位） `targets?`（=target/targets） `TP`/`Tp`/`tp`（+可选一位数字） | `1474` |
| 止盈计数（可疑判定） | `止盈` `目标位?` `targets?` `TP` | `1569` |
| 区间连接词 | `到` `至` `~` `～` `—` `–` `-`（+`区间`/`之间`） | `1501-1503` |
| 分批建仓 | `继续` `分批` `分次` `分笔`；`跌到` `涨到` `到` `第一笔` `第1笔` `首笔` `第二笔` `第2笔` | `1518-1526` |
| 百分比止损 | `带` `带个` `的` `%` `止损` | `1539-1541` |
| 点数 | `点` `个` + `止损`/`止盈`/`目标位?`/`stop`/`SL`/`TP` | `2249-2250` |
| 条件单 | `等待` `等到` `跌破` `站稳` `收回` `站上` `突破` `破位` `若` `如果` `一旦` `触及` `达到` `回到` `确认后` `回调后` `回踩后` `等…再` `等…后` | `2235-2236` |
| 结单/止损通报 | `trade closed` `stop loss hit` `sl hit` `stopped out` `tp N hit` `take profit hit` `target hit` `hit at` `closed at` `profit taken` `止盈(达成/已到/命中/触发/到位)` `止损(达成/已到/被扫/触发/到位)` `已平仓` `平仓完成` `结单` `全部平仓` `已止盈` `已止损` | `1904-1908` |
| 通报 vs 指令（管理类） | `TP N hit/nailed/done/reached/filled` `take profit hit/reached` `breakeven hit` `止盈…达成/到了/命中/触发/已到` `保本…止损/离场`；平仓类：`out of` `close(d)` `closing` `exit(ing)` `stopped out` `stop(ped) out` `fully closed` `平仓` `清仓` `全部走` `先走` `离场` `走人` | `4800-4801` |
| 头仓/试仓（与分批冲突用） | `头仓` `首仓` `底仓` `试仓` | `2574` |
| 结单通报的补充 | `TPs above` `止盈在?上方` `止盈位在上方`（→ `tp_on_chart`，无人使用） | `1565` |
| 审批回复 | `开` `开仓` `开单` `开吧` `可以开` `确认开` `确认` `下单` `建仓` `买入` `买` `执行` `不开` `别开` `不要` `作废` `全部开` `全开` `都开` `全部不开` `全不开` `都不开` `都不要` `全部作废`；判定用 `只开` `只买` `只要` `仅开` `仅买` | `3181-3182`、`3282`、`3291`、`3298` |
| 指令（`KEY`） | `帮助` `状态` `持仓情况` `持仓` `全部平仓` `确认全部平仓` `平仓` `减仓` `修改止损` `移保本` `暂停` `继续` `修改监控群` `修改金额` `修改杠杆` `测试模式` `实盘模式` `挂单情况` `挂单` `待确认` `修改持仓上限` `持仓上限` `进入测试模式` `进入实盘模式` `重新对账` `对账` `解除手工仓` `手工仓已平` | `3378-3381` |
| 指令群兜底回话 | `开` `关` `仓` `单` `平` `止` `损` `盈` `确认` `模式` `持仓` `状态` `帮助` `撤` `停` `继续` `对账` `手工仓` `金额` `杠杆` `上限` `监控` | `3260` |
| 实盘开关否定词 | `不` `别` `取消` `勿`；`不关` `别关` | `3642-3643`、`3658-3660` |
| 币种别名 | 见 2.3 `_NAME_MAP`（含中文俗称与英文全名） | `2856-2875` |
| 币种换算 | `GOLD` `GOLDUSDT` `XAUT` `SILVER` `OIL` `WTI` `CRUDE` `USOIL` `NATGAS` `GAS` | `1320-1326` |

### 5.2 目前【不支持】的写法（本地正则读不到；只能靠 AI 或人工补齐）

| 写法 | 现状 | 依据 |
|---|---|---|
| **Take Profit / Take Profits 后面跟数字** | 【不支持】`_BOUND` 里只有 `targets?` 和 `TP`，**没有 "take profit"**。`Take Profits 78000` → `tps = []`（已用等价正则复算确认） | `1474`（对比：`Stop Loss` 是支持的 `1439`） |
| **Take（大写 TAKE）撞币种** | 第二遍币种匹配的大小写不敏感、且 `TAKE` **不在** `_AMBIG_TICKERS` 里 → 若 `fapi_symbols.json`（仓库外，我无法核对）里存在 `TAKEUSDT`，则正文里的 `Take Profits` 会被算成一个币种 `TAKE`，把单币消息变成"多币种"→ 触发第 8 节的多币种分支 | `2288-2292`、`1372-1381`、`2264-2293` |
| **Hard（如 "Hard stop"）** | 【不支持】代码里没有任何 `hard` 关键词 | 全文检索无命中 |
| **Limit / 限价** | 【不支持】解析里**没有** "Limit 价格" 的规则；`Limit` 只在真实下单层的计划文案里作为"限价腿"出现（`fmt_real_plan`），与解析无关 | 全文检索无命中 |
| **Risk N%（如 `Risk 0.5%`）** | 【不支持】没有把 `Risk` 当作止损/风险百分比的规则（`RISK` 只出现在 `_AMBIG_TICKERS` 里，作用是"不要把它当币种"）；AI 的 prompt 里也**没有** `Risk` 字段 | `1380`、`1231-1247` |
| **多头 / 空头** | 【不支持】方向词表里有"多单/空单"，**没有**"多头/空头" | `1412`、`1414` |
| **Buy / Sell / 买 / 卖 单独的英文写法** | 方向表里无 `Buy`/`Sell`，且 `SIG_KW` 门口表也没有 → `Buy BTC 77000` 在门口就被跳过 | `4583`、`1412-1415` |
| **Stop（不带 Loss/SL）在门口** | 解析支持 `stop`，但 `SIG_KW` 门口**没有** `Stop` → 纯 `Stop 76000` 的消息进不来 | `4583` vs `1440` |
| **目标位 / 入场 / 加仓 在门口** | 解析支持这些词，但 `SIG_KW` 门口没有 → 这类词单独出现时消息被跳过 | `4583` |
| **止盈 N%** | 【不支持】：会被当成价格数字（见 4.7【现状】） | `1477-1496` |
| **入场在数字后（77000 入场）** | 【不支持】只有"价格/做多/做空"这一种"数字在前"的写法 | `1459-1468` |
| **`Entry`/`入场` 与数字之间夹词** | 【不支持】（该正则没有 `_GAP`） | `1455` |
| **支撑 / 阻力 / 分批止盈的"分批"两字外的说法** | 【不支持】（`分批/分次/分笔` 之外没有别的） | `1518-1519` |

---

## 6. AI 解析

### 6.1 调用条件与优先级开关

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 开关 `AI_FIRST` | `AI_FIRST = [True]` | `1935` | 【动态】`runtime_config.json` 的 `ai_first_parse`（`3020-3021`） |
| **AI 优先=开（默认）** | 每条进入解析的消息（单币路径）**无条件**调用 AI（与读图**并行**），然后 `merge_parse_results(txt, AI, 正则)` | `4730-4743` | 正则降级为"交叉校验 + 补档" |
| 多币种路径 | 每一段也调 AI：`merge_parse_results(_seg, parse_text(_seg), _cf)` | `4651-4656` | — |
| **AI 优先=关（老行为）** | 只有 `fast_parse` 返回 `None`（或 `fast_parse_suspect` 为真时把结果置空）才调 AI | `4714-4723`、`4735-4737` | 即"正则自称可疑才叫 AI" |
| AI 输入 | `messages=[{system: prompt}, {user: text[:900]}]`，`model="deepseek-v4-flash"`，`temperature=0` | `1248-1249` | — |
| AI 输出解析 | 用 `re.search(r"\{[\s\S]*\}", content)` 抠 JSON，失败记日志返回 `{}` | `1250-1256` | — |
| 两个模型名（写死在代码里） | 文本 `deepseek-v4-flash`；读图 `deepseek-v4-flash-vision-exp` | `1248`、`765`、`790`、`865`、`2813` | 【现状】`config.example.json` 里有 `deepseek_model` / `deepseek_vision_model`，但代码**没有读**这两个键 |

### 6.2 `parse_text` 的 prompt 原文（逐字抄，`dryrun_bot2.py:1231-1247`）

```text
从这条加密货币跟单消息里抽取开单信息。只输出JSON：{"is_signal":bool,"coin":"大写币种或null","direction":"LONG|SHORT|null","entry":数字或null,"entryRange":[最小,最大]或null,"entry_is_cmp":bool,"add_price":数字或null,"entryLegs":[数字]或null,"stop":数字或null,"stopRange":[小,大]或null,"stopPct":数字或null,"targets":[数字],"targetRanges":[[小,大]]或null,"tp_on_chart":bool,"type":"open|manage|info","manage_action":"close_all|trim|move_stop_to_cost|null"} 规则：① 只用消息里真实出现的数字，绝不编造；② 止盈写在图上则 tp_on_chart=true 且 targets 为空；③ 开仓价若给的是区间（如「在4360到4310区间多」）→ entryRange=[小,大]，entry 留 null；④ 【分批建仓】若给了多个入场点位（如「77777进头仓，76666 75555继续分批接多」或「跌到95第一笔，90第二笔」）→ entryLegs=[点位1,点位2,...] 按出现顺序，entry 留 null；⑤ 止损若给的是区间 → stopRange=[小,大]；⑥ 【百分比止损】若写「带个3%止损」「3%止损」→ stopPct=3（只填数字），stop 留 null；⑦ 止盈若是区间（如「止盈2400到2350」）→ targetRanges=[[2350,2400]]，不要塞进 targets；⑧ 一档止盈只给一个数就放 targets；多个止盈按顺序放 targets。重要：方向必须按原文判断（做多/多/空/做空/LONG/SHORT）。
```

（原文是一整行、无换行；我这里为了排版拆成了多行，字符串内容一字未改——它由 `1231-1247` 的多个字符串字面量拼接而成。）

### 6.3 反幻觉校验 `verify_ai_numbers`

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 收哪些数字 | `_ai_numbers` 递归收取数值，但**跳过** `type` `manage_action` `direction` `coin` `is_signal` `tp_on_chart` `entry_is_cmp` `_fast` `_ai` `_ai_unverified`；最大深度 3 层 | `1938-1956` | — |
| 校验方式 | 先把原文去掉 `,`、`$`、`，`；每个数字要满足：`"%.8g" % v` 是原文子串，或（整数时）`str(int(v))` 是原文子串 | `1963`、`1974-1987` | 【现状】`%.8g` 会做**科学计数法/去尾零**格式化（如 6.50 → `6.5`），因此"原文写 6.50、AI 返回 6.5"能被认成有据可查 |
| 缩写展开 | 原文里的 `74K`/`1.2M`（`([0-9]*\.?[0-9]+)\s*([KkMm])`）会被展开成 74000 / 1200000，用**数值相等**（容差 `max(|e|*1e-9,1e-9)`）而不是子串判定 | `1964-1972`、`1981-1984` | 防止把"原文写 74K、AI 返回 74000"误判成编造 |
| 没通过怎么办 | 记入 `out["_ai_unverified"]`，出单前**强制送人工审批**（绝不自动开） | `1998-2001`、`2586-2589` | — |

### 6.4 `merge_parse_results`：AI 与正则的合并规则

| 顺序 | 规则 | 位置 |
|---|---|---|
| ① | AI 无结果：有正则就用正则（"降级用本地正则，仍走同一套校验与审批"）；两者都无 → 返回 `(None, 说明)` | `1992-1995` |
| ② | 以 AI 结果为基础 `out = dict(ai)` | `1996` |
| ③ | 反幻觉校验 → 不过就标 `_ai_unverified` + 写说明（不丢弃 AI 的其它字段） | `1998-2001` |
| ④ **正则补齐止盈档** | 取正则的 `targets`（`ft`）与 AI 的 `targets`（`at`）；**只有当 `len(ft) > len(at)`** 时，把"AI 没有的那些档"**追加**到 AI 的后面：`out["targets"] = at + _add`，并在说明里写"正则补齐止盈档 +[...]" | `2002-2009` |
| ⑤ 关键字段补漏 + 防冲突注入 | 只对 `("stop", "entry", "entryLegs", "entryRange", "stopRange", "stopPct", "add_price")` 这些键补：**AI 为 None/空** 且 正则非空 才补；但若 AI 已经给出**语义等价**的字段（`_SEM`：`stop/stopRange` 互斥、`entry/entryRange/entryLegs` 三者互斥）就**跳过注入**并记说明 | `2010-2026` |
| ⑥ 方向/币种兜底 | `direction` 为空 → 用正则的；`coin` 为空 → 用正则的 | `2028-2035` |
| ⑦ 标记 | `out["_ai"] = True` | `2027` |
| 返回 | `(info, 说明字符串)`，说明会打进日志 `🤖 [AI优先] …` | `2036`、`4741-4743` |
| 额外的第二次合并 | 单币路径在 AI 之后还有一段：若 `len(正则.targets) > len(info.targets)` → **直接替换**为 `_fast["targets"]`；`stop`/`entry` 为空则用正则补 | `4746-4756` | 
| 【现状】 | AI 优先模式下第 ④ 步已经保证 `len(info.targets) >= len(正则.targets)`，所以上面这段"替换"实际很少触发；它主要服务于 AI优先=关 的分支 | `2002-2009`、`4747-4756` |

### 6.5 `fast_parse_suspect`（"正则可疑地少读了"）

`dryrun_bot2.py:1569-1585`：

| 条件 | 判定 |
|---|---|
| `info` 为空 | 可疑（`True`） |
| 原文里止盈关键词个数 `n_kw`（`_TPKW_RE = 止盈\|目标位?\|targets?\|(?:TP\|Tp\|tp)\s?\d?`）> 0 **且** 读到的档数 `got < min(n_kw, TP_TIERS)` | 可疑 |
| 没读到 `stop` | 可疑 |
| 其余 | 不可疑 |

作用：AI优先=关 时，可疑 → 把正则结果置空、改调 AI（`4718-4722`）；AI优先=开 时它**只被算出来、没有参与决策**（`4713` 赋值后未再使用）。

> 自检基准（`5152-5166`）：「3 个止盈关键词/只读到 1 档 → 可疑」「3 个止盈关键词/读到 3 档 → 不可疑」「没读到止损 → 可疑」「没有止盈关键词 → 不可疑（交给 2R 兜底）」。

---

## 7. 校验与拒绝

### 7.1 数值阈值（改这些就能调松紧）

| 常量 | 取值 | 位置 | 作用 |
|---|---|---|---|
| `MAX_ENTRY_DEV` | `0.20`（20%） | `1914` | 开仓价与**当前市价**偏离超过 20% → 判"像是把别的币的价格串过来了" |
| `MAX_STOP_PCT` | `0.40`（40%） | `1919` | 止损距入场价超过 40% → 判"不像真的止损" |
| `MIN_STOP_PCT` | `0.0005`（0.05%） | `1920` | 止损距入场价小于 0.05% → 判"等于没设止损" |
| 同档判定（读图去重） | 相对差 `< 0.003`（0.3%）算同一档 | `1213` |
| 空仓阈值（"就是市价"） | `|entry - mkt| / mkt <= 0.005`（0.5%） | `3972` |
| 追高/追空门槛 | 市价比博主开仓价差 **> 2%** 才改挂限价（`diff < -0.02` 做多 / `diff > 0.02` 做空） | `2448`、`2455` |
| 2R 兜底 | `R_FALLBACK_MULT = 2.0`（盈亏比 2:1）；`R_FALLBACK_PART = 1.0`（到价**全平**） | `1893`、`1895` |
| `TP_TIERS` | `3` | `45` |

### 7.2 `validate_plan`（单币/多币**共用**的公共校验，`3988-4032`）

返回 `(问题字符串 or None, 修正后的 tps)`；问题字符串面向用户、直接进审批原因。

| 序 | 检查项 | 规则 | 位置 |
|---|---|---|---|
| ① | **止损方向** | 由 `_stop_side_reason` 判定，见 7.3 | `4002-4007` |
| ② | **止盈方向** | 方向不对的档位**剔除**（做多必须 `> entry`，做空必须 `< entry`） | `4008-4010` |
| ③ | **开仓价与市价严重不符** | `|entry - mkt| / mkt > MAX_ENTRY_DEV(20%)` → 拒绝，文案"像是把别的币的价格串过来了" | `4011-4016` |
| ④ | **止损距离荒谬** | `> 40%` → "不像真的止损"；`< 0.05%` → "等于没设止损" | `4017-4023` |
| ⑤ | **止盈已被市价越过** | 做多 `t <= mkt` / 做空 `t >= mkt` → "信号已过期，或价格张冠李戴" | `4024-4029` |
| ⑥ | 条件单 | **不在这里硬拒**，改用 `soft_ask_reason` 转成审批原因 | `4030-4031` |
| 变量清洗 | 先把 `tps` 里非数值的项丢掉 | `4000` | — |

### 7.3 止损方向的归因 `_stop_side_reason`（"陈旧"还是"解析错"，`3951-3985`）

| 判定 | 结果文案要点 | 位置 |
|---|---|---|
| 做多且 `stop >= entry`，或做空且 `stop <= entry` | 才进入"错误一侧"判定 | `3964-3967` |
| `entry_is_cmp` 为真 **或** `|entry - mkt|/mkt <= 0.5%` | → "市价 X **已跌破**信号的止损 Y —— 这条信号已经**失效（陈旧）**…**不下单**" | `3977-3980` |
| 否则（入场价离市价超过 0.5%） | → "止损价 X **不低于** 入场价 Y —— 做多的止损必须在下方，**不下单**（判为解析错误）"；若正文命中 `_CLOSE_ANNOUNCE`，追加"而且这条读起来像**平仓/止损通报**而不是开仓信号，八成是解析错了" | `3981-3985` |
| 【现状·对比注释】 | `finalize_pending` 里的对应位置也走同一函数（`2527-2538`），**不再**一律写"解析错误" | `2527-2529` |

### 7.4 `finalize_pending` 里的其它拒绝/送审分支（单币路径，按代码顺序）

| 序 | 条件 | 动作 | 位置 |
|---|---|---|---|
| 1 | 已暂停 `PAUSED` | 只记录、不出单 | `2390-2392` |
| 2 | 拿不到币安实时价 | 发"待确认"提醒后丢弃 | `2402-2406` |
| 3 | 既没止损也没止盈 | 发提醒"等你确认后我再挂单"，丢弃 | `2480-2483` |
| 4 | 止损在错误一侧 | 发"【信号·拒绝】…"并丢弃 | `2527-2538` |
| 5 | 止盈方向不对 | 剔除这些档（不拒绝整条） | `2539-2544` |
| 6 | 开仓价偏离市价 >20% | 送审批（原因=偏离） | `2548-2552` |
| 7 | 止损距入场 >40% 或 <0.05% | 送审批 | `2553-2558` |
| 8 | 有止盈已被市价越过 | 送审批（"信号已过期…"） | `2559-2564` |
| 9 | `find_all_coins(texts[0])` ≥ 2 个币 | 送审批（"笼统总结，无法确定每个价格属于哪个币"） | `2565-2569` |
| 10 | legs 有值 **且** 正文含 `头仓\|首仓\|底仓\|试仓` | 送审批（"无法确定总共分几笔、每笔多少保证金"） | `2572-2576` |
| 11 | 条件单（`_conditional_order`） | 送审批 | `2580-2582` |
| 12 | 发生过点数换算 | 送审批 | `2583-2585` |
| 13 | AI 反幻觉没通过（`_ai_unverified`） | 送审批 | `2586-2589` |
| 14 | 总敞口超限 | 发"【信号·熔断】"并丢弃（**不是**问审批） | `2613-2621` |
| 15 | 已达持仓上限 | 并入审批询问（回「开」= 提高上限并开单） | `2629-2631` |
| 16 | `REQUIRE_APPROVAL[0]` 为真且未 approved 且非已有持仓 | 送审批："按你的要求：所有订单在开之前都要经你审批（本次已通过全部机器校验）" | `2632-2633` |
| 17 | 该币已有持仓 | 不重复开单；只用来**更新止盈**（更新前按**持仓自己的**方向/入场价重新校验） | `2638-2725` |

### 7.5 `soft_ask_reason`（"不是解析错误、但必须人来判断"，`4035-4045`）

| 触发 | 文案要点 | 位置 |
|---|---|---|
| `_conditional_order(txt)` 为真 | "这条消息是【条件触发】型的（等跌破/站稳/突破/收回…才进），机器人不能替你盯价格条件" | `4039-4041` |
| `_points_info(txt, stop)` 非空 | "止损「N点」是**点数**不是价格，不能直接当止损价用" | `4042-4044` |
| 都没有 | 返回 `None` | `4045` |

### 7.6 熔断规则（金额口径）

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 连亏笔数熔断 | `MAX_CONSEC_LOSS = 0`（**默认关闭**，只保留计数展示） | `271`；判定 `324-325` | 【动态】`runtime_config.json` 的 `max_consec_loss` |
| 单日净亏绝对金额熔断 | `DAILY_LOSS_LIMIT = 0.0`（**默认关闭**） | `274`；判定 `326-327` | 要用就填数字 |
| **主熔断线** | `LOSS_LIMIT_PCT = [0.20]`：熔断线 = **总敞口上限 × 20%**；当前敞口上限 = `MAX_TOTAL_MARGIN` 或 `MAX_OPEN × MARGIN` | `276`；`_exposure_cap` `287-288`；判定 `328-333` | 【动态】`loss_limit_pct`、`max_total_margin` |
| 触线动作 | `PAUSED = True` + 落盘 + 通知"【风控熔断·已暂停交易】"（仍监控记录；已持仓的止盈止损继续管理） | `334-341` | 恢复：发「继续」 |
| 每笔结单后的统计口径 | 净盈亏 = `realized - fee`；亏损才累加连亏，盈利清零连亏；跨日重置 | `308-318` | — |
| 总敞口怎么算 | `sum(MARGIN * remaining)` | `291-296` | — |
| 开单前的敞口闸门 | 加这一单会让敞口 `> 上限` → "【信号·熔断】…未开" | `2613-2621` | — |
| 【现状】注释与默认值不一致 | `276` 的注释写"现在 7×300=2100U → 熔断线 420U"，但代码默认 `MAX_OPEN = 5`（`44`）→ 实际默认是 `5×300=1500U`，熔断线 **300U**；真实值以 `runtime_config.json` 的 `max_open` 为准 | `44`、`276` | — |

### 7.7 手工仓护栏（你自己的仓，机器人绝不碰）

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 登记 | 启动对账时，"交易所有仓、纸面无记录"的币 → `MANUAL_COINS.add(...)` 并落盘 | `399-411`、`4144-4150` | `state.json` 的 `manual` 字段 |
| 拦截函数 | `manual_guard(coin, action)` → 命中返回拒绝原因；`manual_block` 打日志+告警 | `531-549` | — |
| 生效点 | 开新真仓（`561`）、改止损（`593`）、平/减仓（`602`）、止损数量（`620-622`，**绝不用交易所数量**以免覆盖你的仓） | — | 只在实盘模式下拦（`bexec.LIVE[0]`）；**纸面照常记录/解析** |
| 审批单提示 | 该币在 `MANUAL_COINS` 里 → 审批单加一行"你在币安有 X 的手工仓…这条信号只走纸面" | `2111-2113` | — |
| 解除 | 发「解除手工仓 X」或「手工仓已平 X」（不带币种或写"全部"= 全解除） | `3448-3463` | — |
| 持仓健康自检 | `position_sanity`：找出"止盈/止损落在持仓错误一侧"的仓位（止损用**严格**不等式，因为 TP1 后止损会正好等于开仓价） | `4048-4083` | 启动时通报一次，之后每轮只记日志 |

---

## 8. 多币种

### 8.1 `split_by_coin` 切分规则（`2296-2313`）

| 项目 | 当前规则或取值 | 位置 |
|---|---|---|
| 分隔符 | `re.split(r"[。；;！!？?\n]+", t)` —— **句号、分号、叹号、问号、换行**（**不包含逗号 `，`**） | `2301` |
| 每段找币 | `cs = sorted(find_all_coins(seg))`，取 `cs[0]` | `2305-2308` |
| "第一个币"的含义 | 是**按字母序排第一**的币（`sorted`），**不是**文中最先出现的那个 | `2305`、`2308` |
| 同币第二次出现 | `if c in seen: continue` → 该段**被整段丢弃** | `2309-2310` |
| 该段没有币 | `if not cs: continue` → 该段**被整段丢弃**（价位行往往就是这样丢的） | `2306-2307` |
| 触发条件 | 只有当返回 **≥2 段**时才走多币种分支 | `4645-4646` |

### 8.2 多币种分支的解析与审批（`4645-4710`）

| 项目 | 当前规则或取值 | 位置 |
|---|---|---|
| 每段解析 | `_cf = fast_parse(_seg) or {}`；AI优先=开→ `merge_parse_results(_seg, parse_text(_seg), _cf)`；AI优先=关→ 用正则，若没方向或 `fast_parse_suspect` 为真再调 AI 并合并 | `4650-4662` |
| 没方向 | `continue`（日志"段没解析出方向 → 只记录不询问"） | `4663-4666` |
| 入池与取出 | `merge_pending` 后设 `dir`/`group`，随即 `PENDING.pop(_c, None)` —— 该段**不再走 `finalize_pending`** | `4667-4673` |
| 入场价（校验用） | `_ent = p["entry"]`；若为空 → 用 `legs` 的**平均值** | `4679-4682` |
| 公共校验 | `validate_plan(_c, _dir_, _ent, stop, tps, mkt, texts=[_seg], entry_is_cmp=...)`；同时算 `soft_ask_reason(_seg, stop, _ent)` | `4683-4686` |
| 校验不过 | 发"【信号·拒绝】…**不下单**"并 `continue` | `4689-4694` |
| 通过 | `ask_user(_c, _pp, "多币种消息，已按币拆开；本条解析结果如上，请你单独确认" + 可能的 soft 原因, quiet=True)` → **无条件挂起等审批**（不看 `REQUIRE_APPROVAL`） | `4695-4698` |
| 逐币通知 | "【信号·分币】· X 做多：开仓=… 止损=… 止盈=…"（缺失显示"未读到"） | `4699-4704` |
| 汇总通知 | "【信号·多币种待确认】…共 N 种非空组合"，选项由 `_combo_options` 生成 | `4705-4709` |
| 组合选项规则 | `n < 2` → 只有"· 全部不开"；`n <= 3` → **2^n-1 种非空组合** + "· 全部不开"；`n >= 4` → 退化成逐个单选 + 全部开 + 全部不开 + 提示可自由回复 | `2153-2179` |

### 8.3 多币种分支**不做**的事（【现状】，改规则前必须知道）

| 缺失 | 说明 | 依据 |
|---|---|---|
| 不读图 | 读图（`need_chart`）发生在多币种分支**之后**，该分支 `continue` 掉了 → 多币种消息里的 K 线图**永远不会被读** | `4645-4710` vs `4723-4729` |
| 不走 `finalize_pending` | 因此没有：4 秒合并窗口的"信息齐全提前出单"、止盈按"离入场近→远"排序、2R 兜底止盈、点数换算、止损百分比折算、持仓上限/敞口闸门、持仓更新逻辑 | `4667-4673`（pop 掉）vs `2382-2787` |
| 没有止损时不会推导 | 百分比止损折算与"按第一止盈反推 2:1"只在 `finalize_pending` 里 | `2464-2479` |
| 段落里没有币的价位行会被丢 | 换行也会把消息切段 → 多行书写的价位（`入场 77000` / `止损 76000` / `止盈 78000`）各自成段、段内无币 → 被丢弃，只剩带币种的那一行 | `2301-2307` |
| 【已知缺陷】`Take Profits` 撞币种 | 若 `fapi_symbols.json`（仓库外，我无法核对）里存在 `TAKEUSDT`：第二遍匹配对 `Take`（不区分大小写、`TAKE` 不在 `_AMBIG_TICKERS`）会命中 → 单币消息被判成 2 个币 → 走进本分支（另一个后果：单币路径第 9 条检查也会因此送审批） | `2288-2292`、`1372-1381`、`2565-2569` |

---

## 9. 读图（K 线图上的线/框/标签）

读图入口 `read_chart(path)`（`dryrun_bot2.py:1055-1227`）；只读**第一张**图（`4726`）；结果按文件 sha1 缓存（`2790-2807`）。

### 9.1 颜色判定 `_cls`（`742-749`，**严格字面量**）

| 判定顺序 | 条件 | 返回 |
|---|---|---|
| 1 | `max-min < 40` 且 `mx > 200` | `white` |
| 2 | `max-min < 40` 且 `mx > 110` | `grey` |
| 3 | `r > 150 and g > 90 and b < 110 and r >= g` | `orange` |
| 4 | `r > 150 and g < 100 and b < 110` | `red` |
| 5 | `g > 110 and r < 130 and b < 170` | `green` |
| 6 | 其余（含**蓝色/紫色**等） | `None` = **完全看不见** |

### 9.2 标签（价格文字）识别条件

| 项目 | 当前规则或取值 | 位置 |
|---|---|---|
| 采样区域 | x ∈ `[int(w*0.76), int(w*0.985)]`（右侧价格轴），全高 | `1058-1061` |
| 横段门槛（rect） | 同一颜色类连续长度 **≥ 35 像素**才算一个 rect | `1063-1071` |
| 分组（groups） | 同色、`r.y - g.y2 <= 2`、且 x 区间重叠（允许 5px 误差）→ 归同一组 | `1072-1078` |
| 合并（merged） | 同色、垂直间隔 `0 <= g.y1 - t.y2 <= 32`、x 区间重叠（允许 10px 误差）→ 合并成一个标签区域；**合并后要求宽度 `x2-x1+1 >= 55`** | `1079-1086` |
| 两次独立读数 | ① 拼图批量 OCR（`_ocr_tags_batch`，黄色序号，`837-902`）；② 关键标签**单标签单独 OCR**（`_ocr_one_label`，`773-804`） | — |
| 关键标签范围 | 红色标签（最多 2 个）+ **覆盖率最高的 3 条非红长横线** | `1110-1116` |
| 一致性容差 `two_read_ok` | `tol=0.005`（相对差 ≤0.5% 才算一致）；两次只读到一次→采用那一次（记"只有批量/单标签读数"）；都不一致 → **按未读到处理**（`value=None`） | `807-819`、`1120-1136` |
| 批量 OCR 重试 | 返回条数 ≥ 标签数 → 停；否则若 ≥ 一半 → 不重试（关键标签会单独复核）；不足一半 → 重试一次，最多 2 次 | `885-902` |
| 数字清洗 | 去掉 `,`、`$` 再转 float | `1094` |
| 单调性校验 `drop_nonmonotonic` | 同一张图上"y 越大价格必须越低"；违反时剔除**覆盖率较低**的那个标签（标为"违反价格轴单调性"） | `822-834`、`1137-1145` |
| 模型与温度 | `deepseek-v4-flash-vision-exp`，`temperature=0`；system 提示"STRICT JSON only" | `765`、`790`、`865` |

### 9.3 覆盖率与"线自己的行"

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| `_coverage(px,w,y0,y1)` | 在 x ∈ `[int(w*0.06), int(w*0.72)]` 上按步长 2 采样，像素与背景 `(2,24,21)` 的 `|Δr|+|Δg|+|Δb| > 30` 算"非背景"；返回该区间内**最好一行**的非背景占比（保留 3 位小数） | `751-761` | 横线被 K 线遮挡时覆盖率天然 < 1 |
| `TP_MIN_COV` | `0.55` | `904-910` | 判"这是一条止盈横线"的最低覆盖率（原来是 0.85，实测把 TP1/TP2 判掉了） |
| `_line_y` | 在 `[max(0,yc-25), yc+25]` 行范围内，用 `_coverage` 找"覆盖最好**的那一行**" → 那才是线自己的 y（不能用标签区域中心，会被标签文字往上拽） | `1018-1027` | 返回 `(覆盖率, y)` |

### 9.4 两条读图路径

**路径 A：几何读图（优先，`941-968` + `1146-1197`）**

| 项目 | 当前规则或取值 | 位置 |
|---|---|---|
| 填充色判定 `_fill_kind` | 背景（±30）→ None；`max(r,g,b) > FILL_MAX_BRIGHT(160)` → None（亮的算线/蜡烛，不算框）；`g-r>=6 and g-b>=8 and g>=30` → `green`；`r-g>=8 and r-b>=8 and r>=30` → `red` | `925`、`928-938` |
| `find_fill_boxes` | 逐行找该色的 x（步长 2），行内至少 `max(6, int(w*0.01))` 个像素才算"这一行属于框"；总行数至少 `max(8, int(h*0.015))` 才继续；取**最长的连续 y 段**（相邻行间隔 ≤4）；x0/x1 取该段内 x 中位数 | `941-968` |
| 触发条件 | 必须**同时**找到绿框和红框 | `1150` |
| 角色语义 | 绿框在红框上方 → 做多：`stop_y=红框下边`、`entry_y=红绿交界`、`target_y=绿框上边`；否则做空：`stop_y=红框上边`、`entry_y=红绿交界`、`target_y=绿框下边` | `1153-1157` |
| 价格轴拟合 | 用"覆盖率 ≥ `TP_MIN_COV` 的横线"及其**线自己的 y**（`line_y`）线性拟合，最多剔除 3 个离群点，残差要求 ≤ `0.008` | `1158-1161`、`971-994` |
| 取价优先级 | 先轴换算得到各边的估算价，再在**标签**里挑与估算相差 ≤**1.5%**（`nearest_label_value`）的价位（标签是精确价） | `1004-1015`、`1162-1167` |
| 止盈 | 绿框内、`line_y` 严格落在 `min(entry_y,target_y)+5` 与 `max(...)-5` 之间、`cov ≥ TP_MIN_COV`、且价格在止损正确一侧的横线 → 按离入场远近排序取前 `TP_TIERS-1`(=2) 条，**再并上绿框远端（目标边）那个价** → 最终 ≤3 档 | `1175-1184` |
| 失败回退 | 认出了框但框边价格没读准（stop/target 任一为空）→ 退回标签规则（路径 B） | `1194-1196` |
| 返回标记 | `"mode": "geometry"`，并带 `geo` 诊断信息 | `1193` |

**路径 B：标签规则（旧逻辑，`1198-1227`）**

| 顺序 | 规则 | 位置 |
|---|---|---|
| ① | 没有红色标签 → 直接失败：`"无红色止损标签"` | `1198-1202` |
| ② 止损 | `sl = min(红色标签的 value)`（红色标签里**最小**的那个价） | `1203` |
| ③ 开仓 | `above = 价格 > sl*1.0005` 的所有标签，按价格升序；**`entry = above[0]`**（止损上方最近的那个标签，**任意颜色**） | `1204-1206` |
| ④ 止盈候选 | 从 `above` 里筛：`value > entry*1.0005`、`cov >= TP_MIN_COV`、且与已选档位相对差 ≥0.003（去重） | `1207-1215` |
| ⑤ 档位选择 | 候选 > 3 档时，**先按覆盖率取最长的 3 条**，再按价格升序排 | `1216-1222` |
| ⑥ | `above` 为空 → 失败："止损上方无标签" | `1205` |
| 返回 | `tps_all`（全部候选）与 `tps`（最终 ≤3 档）、`lines`（`above` 全部，含没进 TP 的）、`"mode": "legacy"` | `1223-1227` |

### 9.5 哪些线/颜色**目前没有角色**

| 对象 | 现状 | 依据 |
|---|---|---|
| **白色 / 灰色横线** | **不是开仓价、也不决定方向**。它们只有两个可能用途：①（路径 B）如果它是止损上方→入场价之间最近的标签，可能被当成 `entry`；② 覆盖率 ≥0.55 时可成为**止盈候选** | `1198-1222` |
| **橙色横线** | 同上：无专有角色，只能做止盈候选 | `1198-1222` |
| **绿色横线**（不是色框） | 路径 B 里**无专有角色**（角色由"是不是红色"和覆盖率决定）；路径 A 里角色由绿**框**的边决定，单条绿线本身没有语义（只会作为框内横线当止盈） | `1178-1181`、`1198` |
| **蓝色 / 紫色等** | `_cls` 返回 `None` → **完全检测不到**，等于不存在 | `742-749` |
| **任意颜色但覆盖率 < 0.55** | 不能当止盈（但路径 B 里仍可能被当作 `entry`，因为 `above[0]` 不检查覆盖率） | `1204-1206`、`1212` |
| 图上"其余横线" | 只用于"只识别到图"的通报里作为参考列出（条件：价格 > sl*1.0005 且与各止盈档相对差 ≥0.3%），最多列 4 条 | `1854-1860` |
| 死代码 | `_vision(img_path)`（`763-771`）**定义了但全代码没有任何调用** | 全文检索仅 1 处命中（定义行） |

### 9.6 读图相关的其它数字

| 项目 | 取值 | 位置 |
|---|---|---|
| `FILL_MAX_BRIGHT` | `160` | `925` |
| `TP_MIN_COV` | `0.55` | `910` |
| 轴拟合残差门槛 | `0.008`（相对） | `990` |
| 标签与几何值匹配容差 | `0.015`（1.5%） | `1004` |
| 两次读数容差 | `0.005`（0.5%） | `807` |
| 同档去重（图） | `0.003`（0.3%） | `1213` |
| 批量 OCR 最多尝试 | 2 次 | `886` |
| 单标签 OCR 超时 | 45 秒；批量 180 秒 | `799`、`881` |
| 读图线程等待 | 最多 120 秒 | `4759` |

---

## 10. 审批与下单前的固定数字

### 10.1 固定数字（默认值；可被 `runtime_config.json` 覆盖）

| 项目 | 当前规则或取值 | 位置 | 说明 |
|---|---|---|---|
| 每单保证金 `MARGIN` | `300.0` U | `41` | 【动态】`runtime_config.json` 的 `margin`（`3009-3010`）；指令「修改金额 N」也能改（`3596-3601`） |
| 杠杆 `LEV` | `3` | `42` | 【动态】`leverage`；指令「修改杠杆 N」（`3602-3607`） |
| 名义 `NOTIONAL` | `MARGIN * LEV` = 900U | `43`、`3013` | 每次改金额/杠杆都会重算 |
| 持仓上限 `MAX_OPEN` | `5` 笔 | `44` | 【动态】`max_open`；指令「修改持仓上限 N」钳制在 1~50（`3611`） |
| 止盈档数 `TP_TIERS` | `3` | `45` | — |
| 每单保证金·分批建仓 | `MARGIN / len(legs)`（等分） | `2426-2427` | `3` 个点位 → 每个 100U |
| 每单保证金·不追高/不追空 | 拆成两笔：各 `MARGIN / 2`（150U）；价格分别为 `博主价*1.01` 与 `博主价`（做多）／`博主价*0.99` 与 `博主价`（做空） | `2448-2461` | 触发条件：市价与博主开仓价相差 **>2%** |
| 入场计划默认 | 市价单，一笔，保证金 = `MARGIN` | `2411-2412` | — |
| 手续费 | `FEE_MAKER = 0.0002`、`FEE_TAKER = 0.0005` | `685-686` | 市价开仓记 taker、限价止盈记 maker |
| 影子/实盘总开关 | `binance_exec.LIVE[0]`，非 GET 请求在关闭时**直接拒绝** | `binance_exec.py:50`、`92-96`；同步：`dryrun_bot2.py:3026-3028`、`_set_live` `3063-3080` | 【动态】`live_trading` |
| 【现状】`TEST_MODE` | `True`，但**只影响状态显示**与「测试模式 开/关」指令 | `46-48`、`3445`、`3668` | 因为它已不再绕过持仓上限，`3671` 的通知文案"不受 5 笔上限拦截/超过 5 笔会跳过"是**过期的** |

### 10.2 "所有订单必须经用户审批"的实现位置

| 环节 | 位置 | 说明 |
|---|---|---|
| 开关 | `REQUIRE_APPROVAL = [True]`（`1927`）；由 `runtime_config.json` 的 `require_approval` 覆盖（`3016-3018`） | 默认开启 |
| 单币闸门 | `finalize_pending`：`if REQUIRE_APPROVAL[0] and not p.get("approved") and coin not in open_pos:` → `ask_user(...)` 后 `PENDING.pop()` | `2622-2637` |
| 多币种闸门 | 多币种分支**无条件** `ask_user(..., quiet=True)`（不看开关） | `4695-4698` |
| 把握不准也要问 | 上面第 7.4 节的第 6~13 条都会 `ask_user` | `2545-2589` |
| 用户回「开」 | `_open_asking`：`p["approved"] = True`、`deadline = 0`（放回 `PENDING` 立刻出单）；达上限时同时 `cap_override = True` 并提高上限 | `3151-3164` |
| 用户回「不开」 | `ASKING.pop()`，作废不下单 | `3321-3329`、`3347-3351` |
| 30 分钟不回复 | `expire_asking` 自动作废并通知 | `2371-2379` |
| 重启不丢 | 待确认池落盘到 `state.json` 的 `asking`，启动时恢复（超时的丢弃） | `2053-2061`、`4119-4138` |
| 与审批**无关**的另一道闸门 | 影子/实盘 `LIVE` 开关：关闭时任何非只读请求都被拒 | `binance_exec.py:92-96` |

### 10.3 审批文案模板（`ask_user` + `_approval_lines`）

`ask_user` 组装（`2356-2367`）：

```text
【信号·待你确认】{币种} {做多 LONG | 做空 SHORT}
⚠️ 要你确认的原因：{原因}
──────────────
（_approval_lines 的全部内容）
──────────────
⏱ 从信号发出到推送 共 X.Xs（发现 …/抓图 …/解析 …/读图 …/等齐后续消息 …）
原文：{正文前 180 字}
（空行）
**回复「开」= 按上面这组参数开单；回复「不开」= 作废。**
（{30} 分钟内没回复自动作废）
```

`_approval_lines` 的逐行内容（`2086-2150`）：

| 行 | 内容 | 位置 |
|---|---|---|
| 1 | `币种：X ｜ 方向：做多 LONG/做空 SHORT ｜ 来源群：G` | `2108-2109` |
| 2 | `时间：MM-DD HH:MM:SS` | `2110` |
| 3 | 若该币是手工仓 → `⚠️ 你在币安有 X 的手工仓…这条信号只走纸面` | `2111-2113` |
| 4 | `入场：<价>（来源）`；若只有 CMP 没价位 → `入场：**按市价（博主只写了 CMP，未给具体价位）** ≈ 市价（下单时以当时市价为准）`；否则 `入场：**未读到**` | `2114-2124` |
| 5 | 分批建仓时：`分批建仓：N 个点位 …（保证金等分）` | `2125-2127` |
| 6 | `保证金：300U ｜ 杠杆：3 倍 ｜ 名义：900U` | `2128` |
| 7 | `止损位：X` + `止损点数（不含杠杆）：Δ ｜ 占入场 P%` + `若打止损：亏 Y U（保证金 300U 的 Z%）`；没读到则 `止损位：**未读到**` | `2129-2137` |
| 8 | 每档止盈一行：`止盈N：价 ｜ 预期收益 +P%（含 3 倍杠杆） ｜ 盈亏比 R:1`（最多 3 档）；一档都没有则 `止盈：**未读到**` | `2138-2149` |
| 计算口径 | 止损点数 = `|entry - stop|`；占入场 = 点数/entry；亏损 = `300 × 占比 × 3`；止盈预期收益 = `(tp-entry)/entry × d × 3`；盈亏比 = `|tp-entry| / |entry-stop|` | `2131-2146` |
| 排序 | 止盈档按**离入场近→远**展示；没有入场价时按价格升序 | `2103-2107` |

---

## 附录 A：已知缺陷 / 待确认（现状汇总）

> 全部是"当前行为如实记录"，**不含修法建议**。带 ⚠️ 的是会直接影响出单结果的。

| # | 现状 | 位置 |
|---|---|---|
| 1 | ⚠️ `SIG_KW` 门口表比解析关键词表**窄**：`Stop`/`Target`/`目标位`/`入场`/`加仓`/`多单`/`Buy` 都不在表里 → 这类纯文字消息在门口就"闲聊/无关，跳过" | `4583` vs `1440`、`1455`、`1474` |
| 2 | ⚠️ `_GAP` 没排除 `%`：「止损3%」「带个3%止损」→ `stop = 3`（同时 `stopPct = 3`），百分比折算分支不会触发（要求 stop 为空），通常被"止损距离 >40%"拦到审批 | `1436`、`1541`、`1658-1660`、`2467-2471`、`2553-2558` |
| 3 | ⚠️ 加仓价正则用 `[^0-9]{0,14}`（**可跨标点**），与止损那条已修的 `_GAP` 不一致 → 可能吃到下一句的数字 | `1450` vs `1436` |
| 4 | 加仓价 `add_price` 只存不用：`fmt_plan` 的形参 `add` 在函数体里从未使用 → 通知里从不显示 | `1259`、`2774`、`1625-1627` |
| 5 | `tp_on_chart` 只写不读 → 目前**没有任何作用** | `1565`、`1611-1612`、`1945` |
| 6 | ⚠️ 多币种分支：**不读图**、**不走** `finalize_pending`（无 2R 兜底、无止盈排序、无点数换算、无百分比止损折算、无持仓/敞口闸门） | `4645-4710` vs `2382-2787` |
| 7 | ⚠️ 多币种分币按**换行**也切段：多行书写时"没有币种的行"被整段丢弃（价位行丢失）；同一币的第二段也被丢 | `2301-2310` |
| 8 | ⚠️ "每段只认第一个币"= `sorted(find_all_coins(seg))[0]`，是**字母序第一**，不是文中最先出现 | `2305-2308` |
| 9 | ⚠️ `Take Profits` 里的 `Take` 不在 `_AMBIG_TICKERS`，若 `fapi_symbols.json`（仓库外）有 `TAKEUSDT`，则会被算成一个币种 TAKE（→ 触发多币种分支；单币路径也会因此判"笼统总结"送审批） | `2288-2292`、`1372-1381`、`2565-2569` |
| 10 | `_SYMS` 读不到（`fapi_symbols.json` 缺失/空）时，币种校验恒为"放行"，且 `find_coin_in_text` 的代码兜底失效 | `1344`、`2884-2889` |
| 11 | 中文方向词表会**覆盖**英文模板得到的方向；两个中文词表都命中时永远判**做多**（`if/elif`） | `1412-1415` |
| 12 | 方向缺失时默认**做多**（出单与审批两处） | `2401`、`2355`；图上读币种时也默认 `LONG` `4786` |
| 13 | `merge_pending` 里关于 `entry_is_cmp` 是"死代码"的注释与代码**不一致**：代码现在确实会拷贝该字段 | 注释 `1605-1608` vs 代码 `1609-1610` |
| 14 | 熔断注释写"7×300=2100U → 420U"，但默认 `MAX_OPEN = 5` → 默认熔断线是 **300U** | `276`、`44`、`287-288` |
| 15 | `TEST_MODE` 的指令通知文案"不受 5 笔上限拦截/超过 5 笔会跳过"已过期（代码不再绕过上限） | `3668-3671` vs `46-48` |
| 16 | `_NAME_MAP` 键 `" coinbase"` 带前导空格（靠 `strip()` 才能工作）；`"策略"→MSTR`、`"金"→XAU`、`"油"→CL` 等单字/常用词容易误命中 | `2873`、`2862-2866`、`2881` |
| 17 | `_vision()` 是**死代码**（定义后无人调用） | `763-771` |
| 18 | 代码**没有**读 `config.example.json` 里的 `deepseek_model` / `deepseek_vision_model`；模型名写死在代码里 | `1248`、`765` 等 vs `config/config.example.json:5-6` |
| 19 | 爬网页模式下正文被截断到 1200 字符；送 AI 的正文截断到 900 字符 | `3694`、`1249` |
| 20 | `fast_parse_suspect` 在 AI优先=开 时**只被计算、不参与决策** | `4713` |

---

## 附录 B：一条消息的完整判定顺序

```text
① 取消息（API 每群 ≥2 秒一次 / 浏览器 POLL_SEC=0.5）
② 已在 SEEN 里 → 跳过
③ 正文含 SELF_MARKS（32 条）或 通过webhook/（你的指令：/本次盈亏：/该档盈亏：/累计：/invited/test notification → 跳过
④ sender_type=app 且在 CMD_GROUPS → 跳过（自己的通知）
⑤ 在 CMD_GROUPS 且 handle_command 认领（≤60 字 / KEY 开头匹配 / 审批回复）→ 走指令，结束
⑥ 不含 SIG_KW 且 无图 → "闲聊/无关，跳过"（指令群里若像指令则回一句"没认出来"），结束
⑦ 命中 _CLOSE_ANNOUNCE → 只回报持仓或直接忽略，结束
⑧ split_by_coin 得到 ≥2 段 → 【多币种分支】：逐段解析 → 无方向则跳过 → validate_plan → 拒绝或挂审批
   （不走 ⑨ 之后的任何逻辑，也不读图）
⑨ fast_parse（本地正则）
   · AI优先=开：并行调 AI + 读图 → merge_parse_results（AI 为主 + 正则补档 + 反幻觉）
   · AI优先=关：正则命中且不可疑 → 直接用；否则置空并调 AI
⑩ 币种：resolve_coin 校验（不在清单 → 通知"不支持"并结束）；文字没币种则从图（最后一张）读
⑪ 博主管理指令（manage_action）：含"止盈达成"且无平仓字样 → 只回报持仓；否则推【博主指令】并结束
⑫ coin + 方向 + 文字里有信号字段 → 进 PENDING（4 秒窗口；齐全且 ≥1.5 秒可提前出单）
   只有图 → 暂存/并入，4 秒后单独推送，不建可审批单
   都不满足 → 记日志"没通过信号门槛"；看起来像信号则（10 分钟一次）推【信号·未能识别】
⑬ finalize_pending：
   入场计划（市价 / 分批限价 / >2% 不追高追空 / CMP 取市价）
   → 止损推导（百分比伤害 ÷ 第一止盈反推 2:1）
   → 点数换算（强制审批）
   → 止损方向（陈旧 / 解析错误）
   → 止盈方向过滤
   → 价格合理性（偏离 20% / 止损距离 40% 与 0.05% / 止盈已被越过 / 多币种 / 头仓+分批 / 条件单 / 点数 / AI 反幻觉）→ 送审批
   → 止盈按"离入场近→远"排序取 3 档
   → 无止损止盈 → 只提醒
   → 2R 兜底（有开仓价+止损、无止盈）
   → 敞口闸门 → 持仓上限 → 【所有订单必须审批】→ 出单（纸面） + 生成实盘计划
```

---

## 附录 C：我**不确定**的地方

| # | 不确定的内容 | 为什么 |
|---|---|---|
| 1 | `TAKEUSDT` 是否真的存在于币安 USDT-M（= `Take Profits` 会不会真的被当成币种） | 币种清单在 `fapi_symbols.json`，**该文件不在仓库里**（`dryrun_bot2.py:1349`）；我只能确认代码逻辑、不能确认清单内容 |
| 2 | 生产实际生效的 `MARGIN` / `LEV` / `MAX_OPEN` / `groups` / `require_approval` / `ai_first_parse` / `live_trading` / `strict_limit_groups` / `loss_limit_pct` 的具体数值 | 这些都在 `runtime_config.json`（仓库外，`dryrun_bot2.py:2925`）。代码里的只是**默认值**；文档里凡涉及这些我都标了【动态】 |
| 3 | `_SYMS` 在生产上是否非空（即币种校验是否真的在生效） | 取决于服务器上有没有 `fapi_symbols.json`；仓库里没有这个文件，代码读不到时会静默变成"全部放行"（`1347-1355`、`1344`） |
| 4 | `runtime_config.json` 里 `strict_limit_groups` 是否包含「黄金mansoor」，以及"黄金mansoor"这个群现在是否在 `groups` 里 | 同上，都在仓库外的配置文件里（`1918`、`2997-2998`） |
| 5 | 生产用的 DeepSeek 模型名是否与代码写死的一致 | 代码写死 `deepseek-v4-flash` / `deepseek-v4-flash-vision-exp`（`1248`、`765`），但 `config.example.json` 里另有 `deepseek_model` / `deepseek_vision_model` 两个键**代码不读**；实际用哪个取决于代码（即写死的那个） |
| 6 | `_CLOSE_ANNOUNCE` 的"止盈达成"分支在真实消息上的命中率/误判率 | 需要跑生产 `run.log` 才能统计；本仓库只有代码，我无法回放真实消息 |
| 7 | AI（`deepseek-v4-flash`）对 prompt 的实际遵从程度（例如它会不会真的按"① 只用消息里真实出现的数字"填字段） | 这是模型行为，不在代码里；代码只能做事后校验（反幻觉，`1959-1987`） |
| 8 | 我**没有运行**这些正则跑真实消息集 | 本机没有可用的 Python 环境（`python.exe` 是 Windows 应用商店占位程序），所以我用等价的 JS 正则复算了 5 个关键写法（`止损3%`→3、`Take Profits 78000`→无、`止盈1 0.24`→0.24 等）来交叉验证；其余结论均直接来自代码字面量 |
