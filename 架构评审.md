# 架构评审

| 项 | 内容 |
|---|---|
| 评审对象 | 本仓库：飞书群信号 → 币安 USDT-M 合约跟单机器人 |
| 评审依据 | 逐段阅读 `src/`（重点 `dryrun_bot2.py` 全 7870 行的结构）、`tools/` 目录、`config.py` / `notifier.py` 头部；结合 CodeGraph 全项目符号扫描 |
| 结论 | **业务风控设计是亮点；工程结构（单文件巨石 + 零测试 + 全局可变状态）撑不住继续演进** |
| 未验证 | 静态分析结论，未实际运行验证 |

---

## 一、总评

这个项目的"脑子"和"骨架"要分开评价：

- **脑子（业务风控）很好**：启动前大声失败、实盘开关直读 runtime_config.json、持仓健康自检、停机回补闸门、手工仓护栏、可核验日志（干净时也留一行"自检通过"）。注释里逐条记录事故病根，这种习惯非常专业。
- **骨架（工程结构）有硬伤**：所有核心逻辑挤在一个 7870 行的文件里，没有 pytest/unittest 这类可断言的自动化测试兜底，全局可变状态满天飞。风控防的是"逻辑错了会亏钱"，工程结构防的是"改代码时把对的改坏"——目前后者的保障很薄。

> ⚠️ 更正（自审核后）：初稿此处写成"零测试覆盖"，**说重了**。项目实际有一套自建自检体系：`tools/local_selftest.py`（本机桩模块 + /tmp 沙箱）与 `tools/safe_selftest.py`（服务器指纹比对隔离验证），覆盖解析/止盈/审批/B5/B1 等分支。准确的说法是：**有自检，但没有断言式单测**——自检靠"跑起来人工看输出"，且本机自检明确声明"不验证隔离"（历史上就出过自检分支写生产路径、真发飞书的事故）。结论方向不变：缺的是改完代码能**自动报警**的测试。

---

## 二、问题清单（按严重程度排序）

### P1 · 巨型单文件：`src/dryrun_bot2.py` 7870 行、约 170 个函数

一个文件里塞了：取消息（API + 浏览器爬网页两套）、读图（几何 + LLM 两套）、文本解析、AI 解析、审批池、风控、持仓管理、指令系统、通知格式化、页面 JS 字符串。

**真实后果（代码注释自己就记了一笔）：**

> "主循环曾被误缩进到这个 guard 里面，API 模式下会『启动完就退出』→ pm2 无限重启"

这种 bug 的根源就是 `main()` 函数几百行、缩进层级太深，人眼分不清哪行属于哪层。

### P2 · 缺断言式自动化测试

CodeGraph 全项目扫描：`main` 及所有核心函数均标为 `⚠️ no covering tests found`（指无 pytest/unittest 级别的测试）。

需要澄清：项目**并非毫无验证手段**——`tools/local_selftest.py` + `tools/safe_selftest.py` 构成一套自检体系，覆盖解析、止盈、审批等分支，意识很好。但它们是"跑起来人工看输出"，不是"断言失败自动报警"：

- 自检通过与否靠人眼判断，改完代码不会自动跑；
- 本机自检用桩模块替换第三方库，**明确声明不验证隔离**（文件头自述曾出过：自检分支没重定向路径 → 服务器上写生产日志、真发飞书）。

对一个**动真钱的交易机器人**，`validate_plan`、币种归一化、价格精度这类**纯函数**最适合补标准单测——不碰网络、可断言、能进 CI。这是当前最大的风险敞口。

### P3 · 全局可变状态满天飞

`RISK`、`ASKING`、`SEEN`、`API_CURSOR`、`PAUSED[0]`（用单元素列表装布尔值，是为了绕开 Python 局部变量限制）、`RUNTIME_MTIME[0]`、`CHART_READER`……模块级全局变量谁都能改，改没改对只能靠跑起来看。

### P4 · 测试工具靠"改写模块内部变量"实现隔离

代码注释明说：自检脚本通过 `eval_charts.py 设 bot.RUN` 这类方式，直接篡改主程序的全局变量，把生产路径重定向到 /tmp。这是很脆弱的耦合——主程序一改名，工具就悄悄失效。

### P5 · 配置硬编码在主文件里

```python
GROUPS = ["机器人开单通知", "暴富龙", "UA-nurseneil2", "颜驰2群"]
MARGIN = 300.0
LEV = 3
MAX_OPEN = 5
```

虽然 runtime_config.json 已有热加载机制，但群名、保证金、杠杆这类仍写死在代码顶部——改配置要动代码。

### P6 · 异常吞噬过宽，与自己的铁律矛盾

启动恢复状态那段（`src/dryrun_bot2.py` 约 5019 行）：

```python
except Exception:
    pass
```

state.json 坏了就当没事，静默丢掉持仓/游标/风控计数的恢复。项目自己的铁律是"绝不静默丢弃"，这里违背了。

### P7 · tools/ 版本杂乱、无清理约定

`scrape_v16.py`、`scrape_v19.py` 这类带版本号的文件并存，说明演进靠"复制一份改"，旧脚本去留没有约定，新人分不清哪个是现行的。

### P8 · 双模式分支贯穿全文件

`if _api_ok else`（API 取消息 / 浏览器取消息）散落在 7870 行各处。如果抽成"取消息层"一个统一接口，主文件能瘦一大圈，分支成本也从"到处都是"收敛为"一处"。

---

## 三、说句公道话

以下设计比很多正经交易系统还细，**应予保留，重构时不要动坏**：

| 设计 | 为什么对 |
|---|---|
| 启动前大声失败（缺配置 → log + notify + exit 2） | 密钥缺失静默降级是真实出过的事故 |
| 实盘开关直读 runtime_config.json 而非 `bexec.LIVE` | binance_exec 可能没导入成功，读它会永远读不到"用户要实盘" |
| 持仓健康自检（止盈/止损落错一侧会被误判成交） | 防的是"监控循环当场判成假成交"这类真事故 |
| 停机回补闸门（30 分钟时效 + 50 条上限） | 防止重启后灌一堆过期信号 |
| 手工仓护栏 | 机器人对用户手工仓零干涉 |
| 可核验日志（自检干净也留一行"✅ 通过"） | 让"检查跑没跑过"能从日志上证明 |

---

## 四、建议（按性价比排序）

1. **先不拆文件，先补"特征测试"**——对 `validate_plan`、币种归一化、价格精度换算、`position_sanity` 这类**纯函数**写测试（不碰网络，最好测）。花小钱防大事故，也是后面一切重构的前提。
2. **把 `GROUPS` / `MARGIN` / `LEV` / `MAX_OPEN` 挪进 runtime_config.json**，和已有热加载机制统一，改配置不再动代码。
3. **修掉启动恢复段的 `except: pass`**：至少 log 一行"state.json 解析失败，已按空状态启动"，让静默降级变成可核验的降级。
4. **有空时按"取消息 / 解析 / 执行 / 持仓管理 / 通知"切成 5 个模块**——不动逻辑，只搬家。⚠️ 纯搬家也容易搬出错，**只建议在有了第 1 条的测试兜底之后做**。

---

## 五、一句话总结

**脑子（风控）很好，骨架（工程结构）撑不住再长胖了。下一步最划算的不是重构，是补测试。**
